-- Esquema PostgreSQL de EOS · generado por translate_schema_to_pg.py
-- 190 tablas, 31 FK, 235 indices

-- tabla: actividades_sala
CREATE TABLE actividades_sala (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            area_id INTEGER NOT NULL,
            operario_id INTEGER NOT NULL,
            tipo TEXT NOT NULL CHECK(tipo IN
                ('produccion','dispensacion','envasado','acondicionamiento',
                 'conteo_ciclico','limpieza','mantenimiento','otro')),
            descripcion TEXT,
            produccion_id INTEGER,
            inicio_at TEXT NOT NULL DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS')),
            fin_at TEXT,
            duracion_min INTEGER,
            observaciones TEXT,
            creado_por TEXT
        );

-- tabla: agent_memory
CREATE TABLE agent_memory (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            key TEXT NOT NULL,
            value TEXT NOT NULL,
            category TEXT NOT NULL DEFAULT 'general',
            created_by TEXT,
            created_at TEXT NOT NULL DEFAULT (to_char((now() AT TIME ZONE 'UTC'),'YYYY-MM-DD HH24:MI:SS')),
            updated_at TEXT NOT NULL DEFAULT (to_char((now() AT TIME ZONE 'UTC'),'YYYY-MM-DD HH24:MI:SS')),
            UNIQUE(key)
        );

-- tabla: alertas
CREATE TABLE alertas
                 (id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
                  material_id TEXT, material_nombre TEXT, stock_actual DOUBLE PRECISION,
                  stock_minimo DOUBLE PRECISION, fecha TEXT, estado TEXT);

-- tabla: animus_caja_menor
CREATE TABLE animus_caja_menor (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            fecha           TEXT NOT NULL,
            tipo            TEXT NOT NULL CHECK(tipo IN ('ingreso','egreso')),
            concepto        TEXT NOT NULL,
            monto           DOUBLE PRECISION NOT NULL,
            metodo          TEXT DEFAULT 'efectivo',
            referencia      TEXT DEFAULT '',
            observaciones   TEXT DEFAULT '',
            registrado_por  TEXT,
            fecha_creacion  TEXT DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS'))
        );

-- tabla: animus_config
CREATE TABLE animus_config (
        clave TEXT PRIMARY KEY,
        valor TEXT,
        actualizado TEXT DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS'))
    );

-- tabla: animus_contenido_generado
CREATE TABLE animus_contenido_generado (
        id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
        sku TEXT,
        tipo TEXT,
        plataforma TEXT,
        tono TEXT,
        contenido TEXT,
        usado INTEGER DEFAULT 0,
        generado_por TEXT,
        creado_en TEXT DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS'))
    );

-- tabla: animus_conteos_asignados
CREATE TABLE animus_conteos_asignados (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            sku TEXT NOT NULL,
            fecha_asignado TEXT NOT NULL DEFAULT (to_char(now(),'YYYY-MM-DD')),
            asignado_a TEXT,
            estado TEXT NOT NULL DEFAULT 'pendiente'
                CHECK(estado IN ('pendiente','contado','omitido')),
            cantidad_fisica INTEGER,
            cantidad_esperada INTEGER,
            diferencia INTEGER,
            motivo_diferencia TEXT,
            contado_en TEXT,
            creado_en TEXT NOT NULL DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS'))
        );

-- tabla: animus_conteos_ciclicos
CREATE TABLE animus_conteos_ciclicos (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            sku                TEXT NOT NULL,
            producto_nombre    TEXT,
            fecha_conteo       TEXT NOT NULL,
            cantidad_shopify   INTEGER DEFAULT 0,
            cantidad_fisica    INTEGER NOT NULL,
            diferencia         INTEGER NOT NULL,
            explicacion        TEXT,
            registrado_por     TEXT,
            fecha_creacion     TEXT DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS'))
        , aplicado INTEGER DEFAULT 0, movimiento_id_ajuste INTEGER);

-- tabla: animus_ghl_contacts
CREATE TABLE animus_ghl_contacts (
        id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
        ghl_id TEXT UNIQUE,
        nombre TEXT,
        email TEXT,
        telefono TEXT,
        etiquetas TEXT,
        pipeline_etapa TEXT,
        valor_oportunidad DOUBLE PRECISION DEFAULT 0,
        fuente TEXT,
        creado_en TEXT,
        synced_at TEXT DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS'))
    );

-- tabla: animus_ghl_oportunidades
CREATE TABLE animus_ghl_oportunidades (
        id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
        ghl_id TEXT UNIQUE,
        contacto_nombre TEXT,
        pipeline TEXT,
        etapa TEXT,
        valor DOUBLE PRECISION DEFAULT 0,
        estado TEXT,
        creado_en TEXT,
        synced_at TEXT DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS'))
    );

-- tabla: animus_instagram_posts
CREATE TABLE animus_instagram_posts (
        id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
        instagram_id TEXT UNIQUE,
        tipo TEXT,
        descripcion TEXT,
        url_media TEXT,
        url_permalink TEXT,
        likes INTEGER DEFAULT 0,
        comentarios INTEGER DEFAULT 0,
        alcance INTEGER DEFAULT 0,
        impresiones INTEGER DEFAULT 0,
        guardados INTEGER DEFAULT 0,
        publicado_en TEXT,
        synced_at TEXT DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS'))
    );

-- tabla: animus_inventario_baseline
CREATE TABLE animus_inventario_baseline (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            sku TEXT NOT NULL UNIQUE,
            descripcion TEXT,
            unidades_baseline INTEGER NOT NULL,
            fecha_baseline TEXT NOT NULL,
            creado_por TEXT,
            observaciones TEXT,
            creado_en TEXT NOT NULL DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS'))
        );

-- tabla: animus_inventario_movimientos
CREATE TABLE animus_inventario_movimientos (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            sku TEXT NOT NULL,
            tipo TEXT NOT NULL CHECK(tipo IN
                ('ENTRADA','SALIDA','SHOPIFY_VENTA','CONTEO','AJUSTE','BASELINE')),
            cantidad INTEGER NOT NULL,
            fecha TEXT NOT NULL DEFAULT (to_char(now(),'YYYY-MM-DD')),
            origen TEXT,
            referencia TEXT,
            motivo TEXT,
            usuario TEXT,
            creado_en TEXT NOT NULL DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS'))
        );

-- tabla: animus_shopify_customers
CREATE TABLE animus_shopify_customers (
        id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
        shopify_id TEXT UNIQUE,
        nombre TEXT,
        email TEXT,
        telefono TEXT,
        total_gastado DOUBLE PRECISION DEFAULT 0,
        num_pedidos INTEGER DEFAULT 0,
        ciudad TEXT,
        pais TEXT DEFAULT 'CO',
        tags TEXT,
        creado_en TEXT,
        synced_at TEXT DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS'))
    );

-- tabla: animus_shopify_orders
CREATE TABLE animus_shopify_orders (
        id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
        shopify_id TEXT UNIQUE,
        nombre TEXT,
        email TEXT,
        total DOUBLE PRECISION DEFAULT 0,
        moneda TEXT DEFAULT 'COP',
        estado TEXT,
        estado_pago TEXT,
        sku_items TEXT,
        unidades_total INTEGER DEFAULT 0,
        ciudad TEXT,
        pais TEXT DEFAULT 'CO',
        creado_en TEXT,
        synced_at TEXT DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS'))
    , discount_codes TEXT DEFAULT '', subtotal DOUBLE PRECISION DEFAULT 0, total_descuentos DOUBLE PRECISION DEFAULT 0, flujo_synced INTEGER DEFAULT 0, flujo_ingreso_id INTEGER);

-- tabla: area_eventos
CREATE TABLE area_eventos (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            area_id INTEGER NOT NULL,
            tipo TEXT NOT NULL CHECK(tipo IN
                ('estado_cambio','iniciar_prod','terminar_prod',
                 'inicio_limpieza','fin_limpieza','iniciar_conteo','terminar_conteo')),
            estado_anterior TEXT,
            estado_nuevo TEXT,
            produccion_id INTEGER,
            operario_id INTEGER,
            usuario TEXT,
            nota TEXT,
            ts TEXT NOT NULL DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS'))
        );

-- tabla: areas_planta
CREATE TABLE areas_planta (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            codigo TEXT NOT NULL UNIQUE,
            nombre TEXT NOT NULL,
            puede_producir INTEGER NOT NULL DEFAULT 0,
            puede_envasar INTEGER NOT NULL DEFAULT 0,
            marmita_ml INTEGER,
            especial TEXT,
            estado TEXT NOT NULL DEFAULT 'libre',
            activo INTEGER NOT NULL DEFAULT 1,
            orden INTEGER DEFAULT 0,
            creado_en TEXT DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS'))
        , tipo TEXT NOT NULL DEFAULT 'produccion', requiere_limpieza_profunda INTEGER NOT NULL DEFAULT 0, ultima_limpieza_profunda TEXT, zona TEXT NOT NULL DEFAULT 'general');

-- tabla: asistente_acciones_log
CREATE TABLE asistente_acciones_log (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            ts TEXT NOT NULL DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS')),
            usuario TEXT NOT NULL,
            pregunta TEXT,
            tool_invocado TEXT,
            tool_args TEXT,
            tool_resultado TEXT,
            exitoso INTEGER NOT NULL DEFAULT 1
        );

-- tabla: audit_log
CREATE TABLE audit_log (
                 id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
                 usuario TEXT, accion TEXT, tabla TEXT, registro_id TEXT,
                 detalle TEXT, ip TEXT, fecha TEXT, antes TEXT, despues TEXT);

-- tabla: audit_zero_error_runs
CREATE TABLE audit_zero_error_runs (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            fecha TEXT NOT NULL DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS')),
            score_global DOUBLE PRECISION,
            veredicto_global TEXT,
            score_real DOUBLE PRECISION,
            veredicto_real TEXT,
            alta INTEGER DEFAULT 0,
            media INTEGER DEFAULT 0,
            baja INTEGER DEFAULT 0,
            detalles_json TEXT,
            origen TEXT DEFAULT 'cron'
        );

-- tabla: auditorias
CREATE TABLE auditorias (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            tipo             TEXT NOT NULL CHECK(tipo IN ('Interna','Externa','Proveedor','Cliente')),
            ente_auditado    TEXT NOT NULL,
            fecha_planeada   TEXT,
            fecha_ejecutada  TEXT,
            auditor          TEXT,
            alcance          TEXT DEFAULT '',
            hallazgos_count  INTEGER DEFAULT 0,
            no_conformes    INTEGER DEFAULT 0,
            estado           TEXT DEFAULT 'Planeada',
            informe_url      TEXT DEFAULT '',
            creado_en        TEXT DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS'))
        );

-- tabla: ausencias
CREATE TABLE ausencias (
        id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
        empleado_id INTEGER, tipo TEXT, fecha_inicio TEXT, fecha_fin TEXT,
        dias INTEGER DEFAULT 0, estado TEXT DEFAULT 'Pendiente',
        observaciones TEXT, aprobado_por TEXT,
        creado_en TEXT DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS')));

-- tabla: auto_asignacion_log
CREATE TABLE auto_asignacion_log (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            produccion_id INTEGER NOT NULL,
            ejecutado_at TEXT NOT NULL DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS')),
            ejecutado_por TEXT NOT NULL DEFAULT 'auto-ia',
            area_asignada TEXT,
            tanque_asignado TEXT,
            area_envasado_asignada TEXT,
            operarios_json TEXT,
            score INTEGER,
            razon TEXT
        );

-- tabla: auto_plan_cron_state
CREATE TABLE auto_plan_cron_state (
            id INTEGER PRIMARY KEY CHECK(id = 1),
            habilitado INTEGER NOT NULL DEFAULT 0,
            activado_por TEXT,
            activado_at TEXT,
            ultima_ejecucion_at TEXT,
            proxima_ejecucion_at TEXT,
            errores_consecutivos INTEGER NOT NULL DEFAULT 0,
            notas TEXT
        );

-- tabla: auto_plan_runs
CREATE TABLE auto_plan_runs (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            ejecutado_at TEXT NOT NULL DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS')),
            ejecutado_por TEXT NOT NULL DEFAULT 'cron',
            tipo TEXT NOT NULL DEFAULT 'auto'
                CHECK(tipo IN ('auto','manual','dry_run')),
            horizonte_dias INTEGER NOT NULL DEFAULT 60,
            producciones_creadas INTEGER NOT NULL DEFAULT 0,
            compras_creadas INTEGER NOT NULL DEFAULT 0,
            alertas_criticas INTEGER NOT NULL DEFAULT 0,
            emails_enviados INTEGER NOT NULL DEFAULT 0,
            error TEXT,
            payload_json TEXT,
            duracion_ms INTEGER
        );

-- tabla: autoplan_decisiones
CREATE TABLE autoplan_decisiones (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            cliente TEXT NOT NULL,
            producto_nombre TEXT NOT NULL,
            fecha_decision TEXT NOT NULL,
            horizonte_dias INTEGER NOT NULL,
            stock_kg DOUBLE PRECISION,
            velocidad_uds_mes INTEGER,
            ml_unidad INTEGER,
            lote_size_kg DOUBLE PRECISION,
            sugerencia_kg DOUBLE PRECISION,
            sugerencia_fecha TEXT,
            sugerencia_cobertura_dias INTEGER,
            motivo_ia TEXT,
            usuario TEXT,
            -- feedback usuario (NULL hasta que actúe)
            accion_usuario TEXT,
            accion_at TEXT,
            kg_real DOUBLE PRECISION,
            fecha_real TEXT,
            comentario_usuario TEXT,
            -- metadata
            modelo_ia TEXT,
            tokens_usados INTEGER,
            confianza_ia DOUBLE PRECISION,
            payload_completo TEXT
        );

-- tabla: backup_log
CREATE TABLE backup_log (
            id           INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            started_at   TEXT NOT NULL DEFAULT (to_char((now() AT TIME ZONE 'UTC'),'YYYY-MM-DD HH24:MI:SS')),
            completed_at TEXT,
            file_path    TEXT,
            size_bytes   INTEGER,
            status       TEXT NOT NULL DEFAULT 'running',
            error        TEXT,
            triggered_by TEXT NOT NULL DEFAULT 'auto'
        );

-- tabla: bienestar_capacitaciones
CREATE TABLE bienestar_capacitaciones (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            titulo TEXT NOT NULL,
            descripcion TEXT,
            material_tipo TEXT CHECK(material_tipo IN
                ('video','pdf','notebooklm','articulo','otro')),
            material_url TEXT,
            material_notas TEXT,
            asignado_a TEXT NOT NULL,
            asignado_por TEXT NOT NULL,
            fecha_asignacion TEXT NOT NULL DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS')),
            fecha_limite TEXT,
            estado TEXT NOT NULL DEFAULT 'pendiente'
                CHECK(estado IN ('pendiente','en_curso','completada','reprobada','vencida')),
            nota_minima INTEGER DEFAULT 70,
            nota_obtenida INTEGER,
            intentos INTEGER DEFAULT 0,
            completada_en TEXT,
            creado_en TEXT NOT NULL DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS'))
        );

-- tabla: bienestar_capacitaciones_intentos
CREATE TABLE bienestar_capacitaciones_intentos (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            capacitacion_id INTEGER NOT NULL,
            empleado_username TEXT NOT NULL,
            preguntas_json TEXT NOT NULL,
            respuestas_json TEXT,
            evaluacion_json TEXT,
            nota INTEGER,
            iniciado_en TEXT NOT NULL DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS')),
            terminado_en TEXT
        );

-- tabla: calendar_eventos_log
CREATE TABLE calendar_eventos_log (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            evento_id_externo TEXT,
            titulo TEXT NOT NULL,
            fecha TEXT NOT NULL,
            descripcion TEXT,
            kg_detectados DOUBLE PRECISION,
            producto_matcheado TEXT,
            score_match INTEGER,
            estado TEXT NOT NULL DEFAULT 'leido'
                CHECK(estado IN ('leido','matcheado','sin_match','conflicto','manual')),
            notas TEXT,
            ts_leido TEXT NOT NULL DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS'))
        );

-- tabla: calibraciones
CREATE TABLE calibraciones (
                 id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
                 instrumento TEXT NOT NULL,
                 codigo TEXT DEFAULT '',
                 ubicacion TEXT DEFAULT '',
                 fecha_ultima TEXT DEFAULT '',
                 fecha_proxima TEXT NOT NULL,
                 responsable TEXT DEFAULT '',
                 empresa TEXT DEFAULT '',
                 estado TEXT DEFAULT 'Pendiente',
                 certificado TEXT DEFAULT '',
                 observaciones TEXT DEFAULT '');

-- tabla: calibraciones_instrumentos
CREATE TABLE calibraciones_instrumentos
                 (id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
                  instrumento TEXT NOT NULL,
                  codigo TEXT NOT NULL UNIQUE,
                  ubicacion TEXT DEFAULT '',
                  fecha_ultima TEXT DEFAULT '',
                  fecha_proxima TEXT DEFAULT '',
                  responsable TEXT DEFAULT '',
                  empresa TEXT DEFAULT 'Espagiria',
                  estado TEXT DEFAULT 'Vigente',
                  certificado TEXT DEFAULT '',
                  observaciones TEXT DEFAULT '');

-- tabla: calidad_micro_resultados
CREATE TABLE calidad_micro_resultados (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            lote TEXT NOT NULL,
            producto_nombre TEXT NOT NULL,
            fecha_muestreo TEXT,
            fecha_analisis TEXT NOT NULL DEFAULT (to_char(now(),'YYYY-MM-DD')),
            microorganismo TEXT NOT NULL,
            valor DOUBLE PRECISION,
            valor_texto TEXT,
            unidad TEXT NOT NULL DEFAULT 'UFC/g',
            estado TEXT NOT NULL DEFAULT 'ok'
                CHECK(estado IN ('ok','fuera_meta','fuera_industria','observacion')),
            laboratorio TEXT DEFAULT 'Interno',
            analista TEXT,
            metodo TEXT,
            observaciones TEXT,
            oos_id INTEGER,
            creado_por TEXT,
            creado_en TEXT NOT NULL DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS'))
        , envasado_id INTEGER, deadline_resultado TEXT);

-- tabla: calidad_micro_specs
CREATE TABLE calidad_micro_specs (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            producto_nombre TEXT NOT NULL,
            microorganismo TEXT NOT NULL,
            unidad TEXT NOT NULL DEFAULT 'UFC/g',
            limite_industria DOUBLE PRECISION,
            meta_lab DOUBLE PRECISION,
            tipo_limite TEXT NOT NULL DEFAULT 'maximo'
                CHECK(tipo_limite IN ('maximo','minimo','rango','ausencia')),
            metodo_referencia TEXT,
            activa INTEGER NOT NULL DEFAULT 1,
            creado_en TEXT NOT NULL DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS')),
            UNIQUE(producto_nombre, microorganismo)
        );

-- tabla: calidad_micro_specs_default
CREATE TABLE calidad_micro_specs_default (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            microorganismo TEXT NOT NULL UNIQUE,
            unidad TEXT NOT NULL DEFAULT 'UFC/g',
            limite_industria DOUBLE PRECISION,
            meta_lab DOUBLE PRECISION,
            tipo_limite TEXT NOT NULL DEFAULT 'maximo',
            descripcion TEXT
        );

-- tabla: calidad_oos
CREATE TABLE calidad_oos (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            codigo TEXT NOT NULL UNIQUE,
            origen TEXT NOT NULL DEFAULT 'micro'
                CHECK(origen IN ('micro','fisicoquimico','agua','estabilidad','otro')),
            lote TEXT,
            producto TEXT,
            parametro TEXT NOT NULL,
            valor_obtenido DOUBLE PRECISION,
            valor_obtenido_texto TEXT,
            valor_esperado_texto TEXT,
            limite_violado TEXT NOT NULL DEFAULT 'meta_lab'
                CHECK(limite_violado IN ('meta_lab','limite_industria','ambos')),
            fecha_deteccion TEXT NOT NULL DEFAULT (to_char(now(),'YYYY-MM-DD')),
            fecha_objetivo_cierre TEXT,
            fecha_cierre TEXT,
            estado TEXT NOT NULL DEFAULT 'abierto'
                CHECK(estado IN ('abierto','en_investigacion','en_aprobacion','cerrado','rechazado')),
            accion_inmediata TEXT,
            causa_raiz TEXT,
            disposicion TEXT
                CHECK(disposicion IS NULL OR disposicion IN ('liberado','reprocesado','rechazado','destruido','reanalisis')),
            aprobado_por TEXT,
            fecha_aprobacion TEXT,
            capa_id INTEGER,
            creado_por TEXT,
            creado_en TEXT NOT NULL DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS'))
        );

-- tabla: calidad_registros
CREATE TABLE calidad_registros (
                 id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
                 fecha TEXT NOT NULL,
                 tarea_id INTEGER,
                 usuario TEXT DEFAULT '',
                 estado TEXT DEFAULT 'Pendiente',
                 hora_inicio TEXT DEFAULT '',
                 hora_fin TEXT DEFAULT '',
                 valor_registrado TEXT DEFAULT '',
                 observaciones TEXT DEFAULT '',
                 created_at TEXT DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS')));

-- tabla: calidad_sistema_agua
CREATE TABLE calidad_sistema_agua (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            fecha TEXT NOT NULL DEFAULT (to_char(now(),'YYYY-MM-DD')),
            hora TEXT,
            punto_muestreo TEXT NOT NULL,
            tipo_agua TEXT NOT NULL DEFAULT 'purificada'
                CHECK(tipo_agua IN ('purificada','potable','destilada','wfi','grado_reactivo')),
            ph DOUBLE PRECISION,
            conductividad_us_cm DOUBLE PRECISION,
            toc_ppb DOUBLE PRECISION,
            microorganismos_ufc_ml DOUBLE PRECISION,
            cloro_residual_ppm DOUBLE PRECISION,
            temperatura_c DOUBLE PRECISION,
            estado TEXT NOT NULL DEFAULT 'ok'
                CHECK(estado IN ('ok','alerta','fuera_spec')),
            observaciones TEXT,
            operador TEXT,
            creado_en TEXT NOT NULL DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS'))
        );

-- tabla: calidad_tareas
CREATE TABLE calidad_tareas (
                 id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
                 nombre TEXT NOT NULL,
                 categoria TEXT DEFAULT 'General',
                 hora_objetivo TEXT DEFAULT '',
                 hora_limite TEXT DEFAULT '',
                 responsable TEXT DEFAULT 'Jefe CC',
                 procedimiento TEXT DEFAULT '',
                 requiere_valor INTEGER DEFAULT 0,
                 unidad_valor TEXT DEFAULT '',
                 activa INTEGER DEFAULT 1,
                 orden INTEGER DEFAULT 0);

-- tabla: capa_acciones
CREATE TABLE capa_acciones (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            nc_id            INTEGER NOT NULL,
            tipo             TEXT NOT NULL CHECK(tipo IN ('correctiva','preventiva','contencion')),
            descripcion      TEXT NOT NULL,
            responsable      TEXT,
            fecha_compromiso TEXT,
            fecha_ejecucion  TEXT,
            evidencia_url    TEXT DEFAULT '',
            efectiva         INTEGER,
            verificada_por   TEXT,
            fecha_verificacion TEXT,
            estado           TEXT DEFAULT 'Pendiente'
                CHECK(estado IN ('Pendiente','EnEjecucion','Ejecutada','Verificada','Cerrada','NoEfectiva')),
            creado_en        TEXT DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS'))
        );

-- tabla: capa_desviaciones
CREATE TABLE capa_desviaciones (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            codigo TEXT NOT NULL UNIQUE,
            tipo TEXT NOT NULL CHECK(tipo IN ('desviacion','no_conformidad','queja','sugerencia')),
            titulo TEXT NOT NULL,
            descripcion TEXT,
            producto_relacionado TEXT,
            lote TEXT,
            severidad TEXT NOT NULL DEFAULT 'media' CHECK(severidad IN ('alta','media','baja')),
            fecha_apertura TEXT NOT NULL DEFAULT (to_char(now(),'YYYY-MM-DD')),
            fecha_objetivo TEXT,
            fecha_cierre TEXT,
            responsable TEXT,
            accion_inmediata TEXT,
            causa_raiz TEXT,
            accion_correctiva TEXT,
            accion_preventiva TEXT,
            evidencia_url TEXT,
            estado TEXT NOT NULL DEFAULT 'abierta'
                CHECK(estado IN ('abierta','en_investigacion','en_implementacion','cerrada','cancelada')),
            creado_por TEXT,
            creado_en TEXT NOT NULL DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS'))
        );

-- tabla: capacitaciones
CREATE TABLE capacitaciones (
        id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
        nombre TEXT, tipo TEXT, fecha TEXT, duracion_horas DOUBLE PRECISION DEFAULT 1,
        instructor TEXT, empresa TEXT, obligatoria INTEGER DEFAULT 0,
        creado_en TEXT DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS')));

-- tabla: capacitaciones_empleados
CREATE TABLE capacitaciones_empleados (
        id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
        capacitacion_id INTEGER, empleado_id INTEGER,
        completado INTEGER DEFAULT 0, fecha_completado TEXT, calificacion DOUBLE PRECISION,
        UNIQUE(capacitacion_id,empleado_id));

-- tabla: cc_reviews
CREATE TABLE cc_reviews (
                 id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
                 mov_id INTEGER NOT NULL,
                 lote TEXT NOT NULL,
                 codigo_mp TEXT NOT NULL,
                 coa_ok INTEGER DEFAULT 0,
                 lote_coincide INTEGER DEFAULT 0,
                 coa_vigente INTEGER DEFAULT 0,
                 ficha_ok INTEGER DEFAULT 0,
                 solubilidad TEXT DEFAULT '',
                 resultado_aql TEXT DEFAULT '',
                 observaciones_aql TEXT DEFAULT '',
                 muestra_retencion INTEGER DEFAULT 0,
                 observaciones TEXT DEFAULT '',
                 firmante TEXT NOT NULL,
                 estado_final TEXT NOT NULL,
                 fecha TEXT DEFAULT '',
                 ip TEXT DEFAULT '');

-- tabla: chat_messages
CREATE TABLE chat_messages (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            thread_id INTEGER NOT NULL,
            sender TEXT NOT NULL,
            contenido TEXT NOT NULL,
            tipo_mensaje TEXT DEFAULT 'texto'
              CHECK(tipo_mensaje IN ('texto','tarea','compromiso','archivo','imagen','sistema','llamado_atencion')),
            metadata_json TEXT DEFAULT '{}',
            tarea_operativa_id INTEGER,
            compromiso_id INTEGER,
            reply_to_id INTEGER,
            creado_en TEXT DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS')),
            editado_en TEXT,
            eliminado INTEGER DEFAULT 0
        );

-- tabla: chat_reactions
CREATE TABLE chat_reactions (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            message_id INTEGER NOT NULL,
            username TEXT NOT NULL,
            emoji TEXT NOT NULL,
            creado_en TEXT DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS')),
            UNIQUE(message_id, username, emoji)
        );

-- tabla: chat_thread_members
CREATE TABLE chat_thread_members (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            thread_id INTEGER NOT NULL,
            username TEXT NOT NULL,
            rol TEXT DEFAULT 'miembro',
            silenciado INTEGER DEFAULT 0,
            ultimo_leido_id INTEGER DEFAULT 0,
            agregado_en TEXT DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS')),
            UNIQUE(thread_id, username)
        );

-- tabla: chat_threads
CREATE TABLE chat_threads (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            tipo TEXT NOT NULL CHECK(tipo IN ('directo','grupo','broadcast')),
            nombre TEXT DEFAULT '',
            creado_por TEXT NOT NULL,
            creado_en TEXT DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS')),
            ultimo_mensaje_id INTEGER,
            ultimo_mensaje_en TEXT,
            ultimo_mensaje_preview TEXT DEFAULT '',
            activo INTEGER DEFAULT 1
        );

-- tabla: chat_user_presence
CREATE TABLE chat_user_presence (
            username TEXT PRIMARY KEY,
            last_heartbeat TEXT,
            estado TEXT DEFAULT 'desconectado'
              CHECK(estado IN ('conectado','ausente','desconectado')),
            last_thread_visto INTEGER,
            display_name TEXT DEFAULT '',
            avatar_color TEXT DEFAULT ''
        );

-- tabla: checklist_plantillas
CREATE TABLE checklist_plantillas (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            producto_nombre   TEXT,
            item_tipo         TEXT NOT NULL CHECK(item_tipo IN
                ('mp','envase_primario','envase_secundario','tapa',
                 'etiqueta_frontal','etiqueta_posterior','etiqueta_lateral',
                 'serigrafia','tampografia','caja_exterior','instructivo',
                 'estuche','sello','otro')),
            descripcion       TEXT NOT NULL,
            proveedor_default TEXT DEFAULT '',
            dias_anticipacion INTEGER DEFAULT 30,
            obligatorio       INTEGER DEFAULT 1,
            orden             INTEGER DEFAULT 0,
            creado_por        TEXT,
            fecha_creacion    TEXT DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS'))
        );

-- tabla: clientes
CREATE TABLE clientes (
                 id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
                 codigo TEXT UNIQUE, nombre TEXT NOT NULL,
                 empresa TEXT DEFAULT 'ANIMUS', tipo TEXT DEFAULT 'Distribuidor',
                 contacto TEXT DEFAULT '', email TEXT DEFAULT '',
                 telefono TEXT DEFAULT '', nit TEXT DEFAULT '',
                 condiciones_pago TEXT DEFAULT '30 dias', descuento_pct DOUBLE PRECISION DEFAULT 0,
                 activo INTEGER DEFAULT 1, fecha_creacion TEXT, observaciones TEXT DEFAULT '', nivel_aliado TEXT DEFAULT 'Ingreso', semaforo TEXT DEFAULT 'verde', fecha_vinculacion TEXT DEFAULT '', ciudad TEXT DEFAULT '', categoria_profesional TEXT DEFAULT '', canal_captacion TEXT DEFAULT '', redes_sociales TEXT DEFAULT '{}', notas_seguimiento TEXT DEFAULT '', monto_credito_cop DOUBLE PRECISION DEFAULT 0, dias_credito INTEGER DEFAULT 30);

-- tabla: clientes_maquila
CREATE TABLE clientes_maquila (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            nombre TEXT NOT NULL UNIQUE,
            nit_cedula TEXT,
            email TEXT,
            telefono TEXT,
            es_marca_propia INTEGER NOT NULL DEFAULT 0,
            empresa_grupo TEXT,
            comparte_formula_con TEXT,
            margen_seguridad_pct INTEGER NOT NULL DEFAULT 5,
            activo INTEGER NOT NULL DEFAULT 1,
            notas TEXT,
            creado_en TEXT NOT NULL DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS')),
            actualizado_en TEXT
        );

-- tabla: coa_resultados
CREATE TABLE coa_resultados (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            lote             TEXT NOT NULL,
            codigo_mp        TEXT NOT NULL,
            material_nombre  TEXT,
            parametro        TEXT NOT NULL,
            unidad           TEXT DEFAULT '',
            valor_obtenido   TEXT NOT NULL,
            valor_min_spec   DOUBLE PRECISION,
            valor_max_spec   DOUBLE PRECISION,
            conforme         INTEGER DEFAULT 1,
            metodo_ensayo    TEXT DEFAULT '',
            analista         TEXT,
            fecha_analisis   TEXT DEFAULT (to_char(now(),'YYYY-MM-DD')),
            equipo_id        INTEGER,
            observaciones    TEXT DEFAULT '',
            decision         TEXT DEFAULT 'Aprobado',
            creado_en        TEXT DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS'))
        );

-- tabla: cola_liberacion
CREATE TABLE cola_liberacion (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            envasado_id INTEGER,
            producto_nombre TEXT NOT NULL,
            lote TEXT NOT NULL,
            presentacion_etiqueta TEXT,
            unidades INTEGER,
            fecha_envasado TEXT NOT NULL,
            fecha_min_liberacion TEXT NOT NULL,
            estado TEXT NOT NULL DEFAULT 'esperando_micro'
                CHECK(estado IN ('esperando_micro','listo_revisar','liberado','rechazado','reanalisis')),
            micro_resultado_id INTEGER,
            disposicion TEXT
                CHECK(disposicion IS NULL OR disposicion IN ('aprobado','rechazado','reanalizar')),
            aprobado_por TEXT,
            aprobado_at TEXT,
            notas TEXT,
            creado_en TEXT NOT NULL DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS'))
        );

-- tabla: comites_actas
CREATE TABLE comites_actas (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            fecha               TEXT NOT NULL,
            plataforma          TEXT DEFAULT 'Google Meet',
            titulo              TEXT DEFAULT 'Comite Semanal Espagiria',
            asistentes_json     TEXT DEFAULT '[]',
            transcripcion       TEXT DEFAULT '',
            transcripcion_url   TEXT DEFAULT '',
            parseada            INTEGER DEFAULT 0,
            tareas_creadas      INTEGER DEFAULT 0,
            registrado_por      TEXT,
            fecha_creacion      TEXT DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS'))
        );

-- tabla: comprobantes_pago
CREATE TABLE comprobantes_pago (
            id              INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            numero_ce       TEXT UNIQUE NOT NULL,
            anio            INTEGER NOT NULL,
            fecha_emision   TEXT NOT NULL DEFAULT (to_char((now() AT TIME ZONE 'UTC'),'YYYY-MM-DD HH24:MI:SS')),
            pago_oc_id      INTEGER,
            numero_oc       TEXT,
            beneficiario_nombre  TEXT NOT NULL,
            beneficiario_cedula  TEXT,
            beneficiario_banco   TEXT,
            beneficiario_cuenta  TEXT,
            beneficiario_tipo_cta TEXT,
            beneficiario_ciudad  TEXT,
            subtotal        DOUBLE PRECISION NOT NULL DEFAULT 0,
            iva             DOUBLE PRECISION DEFAULT 0,
            iva_pct         DOUBLE PRECISION DEFAULT 0,
            retefuente      DOUBLE PRECISION DEFAULT 0,
            retefuente_pct  DOUBLE PRECISION DEFAULT 0,
            retica          DOUBLE PRECISION DEFAULT 0,
            retica_pct      DOUBLE PRECISION DEFAULT 0,
            total_pagado    DOUBLE PRECISION NOT NULL,
            medio_pago      TEXT,
            observaciones   TEXT,
            pagado_por      TEXT,
            empresa         TEXT DEFAULT 'Espagiria',
            pdf_archivo     TEXT
        );

-- tabla: comprobantes_seq
CREATE TABLE comprobantes_seq (
            anio    INTEGER PRIMARY KEY,
            ultimo  INTEGER NOT NULL DEFAULT 0
        );

-- tabla: compromisos
CREATE TABLE compromisos
                 (id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
                  descripcion TEXT NOT NULL,
                  responsable TEXT DEFAULT '',
                  area TEXT DEFAULT '',
                  fecha_limite TEXT DEFAULT '',
                  estado TEXT DEFAULT 'Pendiente',
                  prioridad TEXT DEFAULT 'Normal',
                  origen TEXT DEFAULT '',
                  empresa TEXT DEFAULT 'Espagiria',
                  fecha_creacion TEXT,
                  fecha_cierre TEXT DEFAULT '',
                  notas TEXT DEFAULT '');

-- tabla: config_facturacion
CREATE TABLE config_facturacion (
        empresa         TEXT    NOT NULL,
        anio            INTEGER NOT NULL,
        tipo            TEXT    NOT NULL DEFAULT 'FV',
        siguiente       INTEGER DEFAULT 1,
        PRIMARY KEY (empresa, anio, tipo)
    );

-- tabla: conteo_ciclico_calendario
CREATE TABLE conteo_ciclico_calendario (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            fecha TEXT NOT NULL,
            material_id TEXT NOT NULL,
            material_nombre TEXT,
            categoria_abc TEXT,
            asignado_a TEXT,
            stock_esperado_g DOUBLE PRECISION,
            stock_real_g DOUBLE PRECISION,
            diferencia_g DOUBLE PRECISION,
            estado TEXT NOT NULL DEFAULT 'programado'
                CHECK(estado IN ('programado','contado','con_diferencia','cerrado','reprogramado')),
            iniciado_at TEXT,
            terminado_at TEXT,
            iniciado_por TEXT,
            terminado_por TEXT,
            notas TEXT,
            generado_por TEXT NOT NULL DEFAULT 'auto',
            UNIQUE(fecha, material_id)
        );

-- tabla: conteo_ciclico_config
CREATE TABLE conteo_ciclico_config (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            material_id TEXT NOT NULL UNIQUE,
            categoria_abc TEXT NOT NULL DEFAULT 'C'
                CHECK(categoria_abc IN ('A','B','C')),
            frecuencia_dias INTEGER NOT NULL DEFAULT 90,
            ultimo_conteo_fecha TEXT,
            ultimo_conteo_diferencia DOUBLE PRECISION,
            requiere_validacion INTEGER NOT NULL DEFAULT 0,
            actualizado_en TEXT
        );

-- tabla: conteo_items
CREATE TABLE conteo_items (
                 id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
                 conteo_id INTEGER NOT NULL,
                 codigo_mp TEXT NOT NULL, nombre_mp TEXT,
                 stock_sistema DOUBLE PRECISION DEFAULT 0,
                 stock_fisico DOUBLE PRECISION DEFAULT NULL,
                 diferencia DOUBLE PRECISION DEFAULT 0,
                 lote TEXT DEFAULT '', zona TEXT DEFAULT '',
                 ajuste_aplicado INTEGER DEFAULT 0,
                 observaciones TEXT DEFAULT '', estanteria TEXT DEFAULT '', causa_diferencia TEXT DEFAULT '', valor_diferencia DOUBLE PRECISION DEFAULT 0, requiere_gerencia INTEGER DEFAULT 0, aprobado_gerencia INTEGER DEFAULT 0, aprobado_gerencia_por TEXT DEFAULT '');

-- tabla: conteos_fisicos
CREATE TABLE conteos_fisicos (
                 id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
                 numero TEXT UNIQUE,
                 fecha_inicio TEXT, fecha_cierre TEXT,
                 estado TEXT DEFAULT 'Abierto',
                 responsable TEXT DEFAULT '',
                 observaciones TEXT DEFAULT '',
                 total_items INTEGER DEFAULT 0,
                 items_diferencia INTEGER DEFAULT 0,
                 aprobado_por TEXT DEFAULT '', estanteria TEXT DEFAULT '', tipo_conteo TEXT DEFAULT 'Ciclico');

-- tabla: control_cambios
CREATE TABLE control_cambios (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            codigo TEXT UNIQUE,
            fecha_solicitud TEXT NOT NULL DEFAULT (to_char(now(),'YYYY-MM-DD')),
            solicitado_por TEXT NOT NULL,
            tipo TEXT NOT NULL DEFAULT 'otro'
              CHECK(tipo IN ('formulacion','proceso','equipo','instalacion',
                              'proveedor','documental','sistema','envase','otro')),
            titulo TEXT NOT NULL,
            descripcion TEXT NOT NULL,
            justificacion TEXT,
            impacto_bpm INTEGER NOT NULL DEFAULT 0,
            impacto_regulatorio INTEGER NOT NULL DEFAULT 0,
            areas_afectadas TEXT,
            severidad TEXT
              CHECK(severidad IN ('mayor','menor') OR severidad IS NULL),
            evaluado_por TEXT,
            evaluado_at TEXT,
            evaluacion_descripcion TEXT,
            aprobado_por TEXT,
            aprobado_at TEXT,
            aprobacion_observaciones TEXT,
            requiere_invima INTEGER NOT NULL DEFAULT 0,
            notificacion_invima_at TEXT,
            notificacion_invima_ref TEXT,
            plan_implementacion TEXT,
            fecha_implementacion_propuesta TEXT,
            responsable_implementacion TEXT,
            implementado_at TEXT,
            implementado_por TEXT,
            verificacion_post TEXT,
            verificado_por TEXT,
            verificado_at TEXT,
            verificacion_ok INTEGER,
            estado TEXT NOT NULL DEFAULT 'solicitado'
              CHECK(estado IN ('solicitado','en_evaluacion','aprobado','rechazado',
                                'en_implementacion','implementado','cerrado')),
            fecha_cierre TEXT,
            cerrado_por TEXT,
            observaciones_cierre TEXT,
            creado_en TEXT NOT NULL DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS')),
            actualizado_en TEXT
        );

-- tabla: control_cambios_eventos
CREATE TABLE control_cambios_eventos (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            cambio_id INTEGER NOT NULL,
            evento_tipo TEXT NOT NULL,
            estado_anterior TEXT,
            estado_nuevo TEXT,
            usuario TEXT,
            comentario TEXT,
            creado_en TEXT NOT NULL DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS'))
        );

-- tabla: cotizaciones
CREATE TABLE cotizaciones (
            id              INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            ronda_id        TEXT NOT NULL,
            proveedor       TEXT NOT NULL,
            fecha_solicitud TEXT NOT NULL DEFAULT (to_char((now() AT TIME ZONE 'UTC'),'YYYY-MM-DD HH24:MI:SS')),
            fecha_recibida  TEXT,
            valor_total     DOUBLE PRECISION DEFAULT 0,
            condiciones     TEXT DEFAULT '',
            descripcion     TEXT DEFAULT '',
            tiempo_entrega_dias INTEGER DEFAULT 0,
            ganadora        INTEGER DEFAULT 0,
            numero_oc       TEXT DEFAULT '',
            archivo         TEXT DEFAULT '',
            creado_por      TEXT NOT NULL DEFAULT '',
            estado          TEXT NOT NULL DEFAULT 'Pendiente'
        );

-- tabla: cron_jobs_health
CREATE TABLE cron_jobs_health (
            job_name TEXT PRIMARY KEY,
            errores_consecutivos INTEGER NOT NULL DEFAULT 0,
            ultimo_error_at TEXT,
            ultimo_error_msg TEXT,
            notificado_at TEXT
        );

-- tabla: cron_jobs_runs
CREATE TABLE cron_jobs_runs (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            job_name TEXT NOT NULL,
            ejecutado_at TEXT NOT NULL DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS')),
            duracion_ms INTEGER,
            ok INTEGER NOT NULL DEFAULT 1,
            resultado_json TEXT,
            error TEXT
        );

-- tabla: cron_locks
CREATE TABLE cron_locks (
            job_name TEXT PRIMARY KEY,
            locked_at TEXT NOT NULL DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS')),
            locked_by TEXT
        );

-- tabla: cronograma_ejecuciones
CREATE TABLE cronograma_ejecuciones (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            cronograma_id INTEGER NOT NULL,
            fecha_planeada TEXT NOT NULL,
            fecha_real TEXT,
            ejecutado_por TEXT,
            evidencia_url TEXT,
            observaciones TEXT,
            estado TEXT NOT NULL DEFAULT 'pendiente'
                CHECK(estado IN ('pendiente','ejecutado','vencido','no_aplica')),
            creado_en TEXT NOT NULL DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS'))
        );

-- tabla: cronogramas_bpm
CREATE TABLE cronogramas_bpm (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            codigo TEXT NOT NULL UNIQUE,
            nombre TEXT NOT NULL,
            descripcion TEXT,
            frecuencia TEXT,
            ejecuciones_year_objetivo INTEGER DEFAULT 12,
            responsable TEXT,
            activo INTEGER NOT NULL DEFAULT 1,
            creado_en TEXT NOT NULL DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS'))
        );

-- tabla: db_health_log
CREATE TABLE db_health_log (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            fecha TEXT NOT NULL DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS')),
            integrity TEXT,
            db_size_kb INTEGER,
            wal_size_kb INTEGER,
            error TEXT,
            origen TEXT DEFAULT 'cron'
        );

-- tabla: despachos
CREATE TABLE despachos (
                 id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
                 numero TEXT UNIQUE, numero_pedido TEXT DEFAULT '',
                 cliente_id INTEGER, fecha TEXT, operador TEXT DEFAULT '',
                 observaciones TEXT DEFAULT '', estado TEXT DEFAULT 'Completado');

-- tabla: despachos_items
CREATE TABLE despachos_items (
                 id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
                 numero_despacho TEXT, sku TEXT, descripcion TEXT,
                 lote_pt TEXT, cantidad INTEGER DEFAULT 0, precio_unitario DOUBLE PRECISION DEFAULT 0);

-- tabla: desviaciones
CREATE TABLE desviaciones (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            codigo TEXT UNIQUE,
            fecha_deteccion TEXT NOT NULL DEFAULT (to_char(now(),'YYYY-MM-DD')),
            hora_deteccion TEXT,
            detectado_por TEXT NOT NULL,
            tipo TEXT NOT NULL DEFAULT 'otra'
              CHECK(tipo IN ('proceso','equipo','instalacion','sistema_agua','ambiental',
                              'documental','personal','materia_prima','envase','otra')),
            area_origen TEXT,
            descripcion TEXT NOT NULL,
            contencion_inmediata TEXT,
            impacto_producto INTEGER NOT NULL DEFAULT 0,
            lotes_afectados TEXT,
            clasificacion TEXT
              CHECK(clasificacion IN ('critica','mayor','menor','informativa') OR clasificacion IS NULL),
            clasificado_por TEXT,
            clasificado_at TEXT,
            justificacion_clasificacion TEXT,
            metodo_investigacion TEXT
              CHECK(metodo_investigacion IN ('5_porques','ishikawa','arbol_decision','otro') OR metodo_investigacion IS NULL),
            causa_raiz_descripcion TEXT,
            investigado_por TEXT,
            investigacion_at TEXT,
            capa_descripcion TEXT,
            capa_responsable TEXT,
            capa_fecha_limite TEXT,
            capa_implementado_at TEXT,
            verificacion_efectividad TEXT,
            verificado_at TEXT,
            verificado_por TEXT,
            efectividad_ok INTEGER,
            estado TEXT NOT NULL DEFAULT 'detectada'
              CHECK(estado IN ('detectada','clasificada','en_investigacion',
                                'capa_propuesto','capa_implementado','cerrada','rechazada')),
            fecha_cierre TEXT,
            cerrado_por TEXT,
            observaciones_cierre TEXT,
            creado_en TEXT NOT NULL DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS')),
            actualizado_en TEXT
        );

-- tabla: desviaciones_eventos
CREATE TABLE desviaciones_eventos (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            desviacion_id INTEGER NOT NULL,
            evento_tipo TEXT NOT NULL,
            estado_anterior TEXT,
            estado_nuevo TEXT,
            usuario TEXT,
            comentario TEXT,
            creado_en TEXT NOT NULL DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS'))
        );

-- tabla: e_signatures
CREATE TABLE e_signatures (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            record_table TEXT NOT NULL,
            record_id TEXT NOT NULL,
            meaning TEXT NOT NULL,
            signer_username TEXT NOT NULL,
            signer_full_name TEXT DEFAULT '',
            signer_cedula TEXT DEFAULT '',
            signer_cargo TEXT DEFAULT '',
            signed_at_utc TEXT NOT NULL,
            ip TEXT DEFAULT '',
            auth_factor TEXT NOT NULL,
            comment TEXT DEFAULT '',
            record_hash TEXT DEFAULT '',
            signature_hash TEXT NOT NULL
        );

-- tabla: ebr_ejecuciones
CREATE TABLE ebr_ejecuciones (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            mbr_template_id INTEGER NOT NULL,
            mbr_version INTEGER NOT NULL,
            produccion_id INTEGER DEFAULT NULL,
            lote TEXT NOT NULL UNIQUE,
            estado TEXT NOT NULL DEFAULT 'iniciado',
            iniciado_por TEXT NOT NULL,
            iniciado_at_utc TEXT NOT NULL,
            completado_at_utc TEXT DEFAULT NULL,
            liberado_por TEXT DEFAULT '',
            liberado_at_utc TEXT DEFAULT NULL,
            liberado_signature_id INTEGER DEFAULT NULL,
            rechazado_motivo TEXT DEFAULT '',
            cantidad_objetivo_g DOUBLE PRECISION NOT NULL,
            cantidad_real_g DOUBLE PRECISION DEFAULT NULL,
            yield_pct DOUBLE PRECISION DEFAULT NULL,
            notas TEXT DEFAULT '', numero_op TEXT DEFAULT NULL
        );

-- tabla: ebr_pasos_ejecutados
CREATE TABLE ebr_pasos_ejecutados (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            ebr_id INTEGER NOT NULL,
            mbr_paso_id INTEGER NOT NULL,
            orden INTEGER NOT NULL,
            descripcion TEXT NOT NULL,
            tipo_paso TEXT DEFAULT 'otro',
            equipo_requerido TEXT DEFAULT '',
            requiere_e_sign INTEGER DEFAULT 0,
            requiere_qc INTEGER DEFAULT 0,
            estado TEXT NOT NULL DEFAULT 'pendiente',
            operario_username TEXT DEFAULT '',
            iniciado_at_utc TEXT DEFAULT NULL,
            completado_at_utc TEXT DEFAULT NULL,
            observaciones TEXT DEFAULT '',
            e_sign_id INTEGER DEFAULT NULL,
            qc_username TEXT DEFAULT '',
            qc_e_sign_id INTEGER DEFAULT NULL,
            desviacion_id INTEGER DEFAULT NULL,
            UNIQUE(ebr_id, orden)
        );

-- tabla: ebr_pesajes
CREATE TABLE ebr_pesajes (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            ebr_id INTEGER NOT NULL,
            ebr_paso_id INTEGER DEFAULT NULL,
            material_id TEXT NOT NULL,
            material_nombre TEXT DEFAULT '',
            cantidad_teorica_g DOUBLE PRECISION NOT NULL,
            cantidad_real_g DOUBLE PRECISION NOT NULL,
            delta_g DOUBLE PRECISION,
            delta_pct DOUBLE PRECISION,
            lote_mp TEXT DEFAULT '',
            pesado_por TEXT NOT NULL,
            pesado_at_utc TEXT NOT NULL,
            e_sign_id INTEGER DEFAULT NULL,
            notas TEXT DEFAULT ''
        );

-- tabla: email_destinatarios_config
CREATE TABLE email_destinatarios_config (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            rol TEXT NOT NULL UNIQUE,
            nombre TEXT,
            email TEXT NOT NULL,
            recibe_resumen_diario INTEGER NOT NULL DEFAULT 1,
            recibe_alertas_criticas INTEGER NOT NULL DEFAULT 1,
            recibe_compras_aprob INTEGER NOT NULL DEFAULT 0,
            recibe_calidad INTEGER NOT NULL DEFAULT 0,
            recibe_agenda_personal INTEGER NOT NULL DEFAULT 0,
            activo INTEGER NOT NULL DEFAULT 1,
            actualizado_en TEXT
        );

-- tabla: empleados
CREATE TABLE empleados (
        id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
        codigo TEXT UNIQUE, nombre TEXT, apellido TEXT, cedula TEXT UNIQUE,
        cargo TEXT, area TEXT, empresa TEXT DEFAULT 'Espagiria',
        tipo_contrato TEXT DEFAULT 'Indefinido', fecha_ingreso TEXT,
        fecha_fin_contrato TEXT, estado TEXT DEFAULT 'Activo',
        salario_base DOUBLE PRECISION DEFAULT 0, eps TEXT, afp TEXT, arl TEXT,
        caja_compensacion TEXT, email TEXT, telefono TEXT,
        nivel_riesgo INTEGER DEFAULT 1, observaciones TEXT,
        creado_en TEXT DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS')), banco TEXT DEFAULT NULL, numero_cuenta TEXT DEFAULT NULL, tipo_cuenta TEXT DEFAULT NULL);

-- tabla: empleados_documentos
CREATE TABLE empleados_documentos (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            empleado_id INTEGER NOT NULL,
            tipo TEXT NOT NULL,
              -- contrato | hoja_vida | examen_medico_ingreso |
              -- examen_medico_periodico | examen_medico_egreso |
              -- afiliacion_eps | afiliacion_arl | afiliacion_afp |
              -- afiliacion_caja | cedula | rut | foto |
              -- certificado_estudios | certificado_laboral_anterior |
              -- libreta_militar | otro
            nombre TEXT DEFAULT '',
            archivo_url TEXT DEFAULT '',  -- ruta al archivo o URL
            archivo_data TEXT DEFAULT '',  -- base64 si es chico
            mime_type TEXT DEFAULT '',
            fecha_emision TEXT,
            fecha_vencimiento TEXT,
            observaciones TEXT DEFAULT '',
            cargado_por TEXT DEFAULT '',
            fecha_carga TEXT DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS'))
        );

-- tabla: envasado
CREATE TABLE envasado (
        id                  INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
        produccion_id       INTEGER DEFAULT 0,
        lote                TEXT NOT NULL,
        producto            TEXT NOT NULL,
        presentacion        TEXT DEFAULT '',
        batch_g             DOUBLE PRECISION DEFAULT 0,
        unidades            INTEGER DEFAULT 0,
        envase_codigo       TEXT DEFAULT '',
        tapa_codigo         TEXT DEFAULT '',
        operador            TEXT DEFAULT '',
        fecha               TEXT DEFAULT '',
        estado              TEXT DEFAULT 'Completado',
        observaciones       TEXT DEFAULT ''
    );

-- tabla: eos_leads
CREATE TABLE eos_leads (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            nombre TEXT,
            email TEXT,
            telefono TEXT,
            empresa TEXT,
            mensaje TEXT,
            fuente TEXT DEFAULT 'web3forms',
            payload_raw TEXT,
            estado TEXT NOT NULL DEFAULT 'nuevo'
                CHECK(estado IN ('nuevo','contactado','demo_agendada','propuesta','cerrado','descartado')),
            owner TEXT,
            notas TEXT,
            creado_en TEXT NOT NULL DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS'))
        );

-- tabla: equipo_limpieza_log
CREATE TABLE equipo_limpieza_log (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            equipo_codigo TEXT NOT NULL,
            lote_anterior TEXT DEFAULT '',
            lote_siguiente TEXT DEFAULT '',
            tipo_limpieza TEXT NOT NULL DEFAULT 'rutinaria',
            operario_username TEXT NOT NULL,
            operario_e_sign_id INTEGER DEFAULT NULL,
            qc_username TEXT DEFAULT '',
            qc_e_sign_id INTEGER DEFAULT NULL,
            visual_ok INTEGER DEFAULT NULL,
            iniciado_at_utc TEXT NOT NULL,
            completado_at_utc TEXT DEFAULT NULL,
            observaciones TEXT DEFAULT ''
        );

-- tabla: equipos_cronograma
CREATE TABLE equipos_cronograma (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            equipo_codigo TEXT NOT NULL,
            anio INTEGER NOT NULL DEFAULT 2026,
            mes INTEGER NOT NULL CHECK(mes BETWEEN 1 AND 12),
            tipo_actividad TEXT NOT NULL
              CHECK(tipo_actividad IN ('preventivo','correctivo','verificacion','calibracion')),
            estado TEXT NOT NULL DEFAULT 'programado'
              CHECK(estado IN ('programado','completado','reprogramado','cancelado')),
            fecha_completado TEXT,
            completado_por TEXT,
            evento_id INTEGER,
            observaciones TEXT,
            creado_en TEXT NOT NULL DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS')),
            UNIQUE(equipo_codigo, anio, mes, tipo_actividad)
        );

-- tabla: equipos_eventos
CREATE TABLE equipos_eventos (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            equipo_codigo TEXT NOT NULL,
            tipo_evento TEXT NOT NULL
              CHECK(tipo_evento IN (
                'calibracion','verificacion_diaria','verificacion_semestral',
                'mantenimiento_preventivo','mantenimiento_correctivo',
                'baja','reparacion','validacion','reactivacion'
              )),
            fecha TEXT NOT NULL DEFAULT (to_char(now(),'YYYY-MM-DD')),
            fecha_proxima TEXT,
            estado TEXT NOT NULL DEFAULT 'completado'
              CHECK(estado IN ('completado','programado','en_curso','cancelado')),
            responsable TEXT,
            empresa_externa TEXT,
            certificado_url TEXT,
            resultado TEXT,
            observaciones TEXT,
            creado_por TEXT,
            creado_en TEXT NOT NULL DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS'))
        );

-- tabla: equipos_planta
CREATE TABLE equipos_planta (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            codigo TEXT NOT NULL,
            nombre TEXT NOT NULL,
            area_codigo TEXT,
            ubicacion_raw TEXT,
            tipo TEXT NOT NULL DEFAULT 'otro',
            capacidad_raw TEXT,
            capacidad_litros DOUBLE PRECISION,
            capacidad_kg DOUBLE PRECISION,
            estado_operacional TEXT NOT NULL DEFAULT 'operativo'
                CHECK(estado_operacional IN ('operativo','mantenimiento','baja','calibracion')),
            activo INTEGER NOT NULL DEFAULT 1,
            notas TEXT,
            creado_en TEXT NOT NULL DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS')),
            actualizado_en TEXT,
            UNIQUE(codigo, ubicacion_raw)
        );

-- tabla: especificaciones_mp
CREATE TABLE especificaciones_mp (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            codigo_mp        TEXT NOT NULL,
            parametro        TEXT NOT NULL,
            unidad           TEXT DEFAULT '',
            valor_min        DOUBLE PRECISION,
            valor_max        DOUBLE PRECISION,
            metodo_ensayo    TEXT DEFAULT '',
            obligatorio      INTEGER DEFAULT 1,
            tipo             TEXT DEFAULT 'fisicoquimico',
            farmacopea_ref   TEXT DEFAULT '',
            creado_por       TEXT,
            fecha_creacion   TEXT DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS')),
            UNIQUE(codigo_mp, parametro)
        );

-- tabla: estabilidades
CREATE TABLE estabilidades (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            producto         TEXT NOT NULL,
            lote_piloto      TEXT NOT NULL,
            condicion        TEXT NOT NULL,
            tiempo_dias      INTEGER NOT NULL,
            tiempo_etiqueta  TEXT,
            fecha_inicio     TEXT NOT NULL,
            fecha_evaluacion TEXT,
            parametros_json  TEXT DEFAULT '{}',
            conforme         INTEGER DEFAULT 1,
            observaciones    TEXT DEFAULT '',
            analista         TEXT,
            estado           TEXT DEFAULT 'Programado'
                CHECK(estado IN ('Programado','Iniciado','En curso','Concluido','Suspendido')),
            creado_en        TEXT DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS'))
        );

-- tabla: evaluaciones
CREATE TABLE evaluaciones (
        id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
        empleado_id INTEGER, periodo TEXT, evaluador TEXT,
        puntaje_total DOUBLE PRECISION, puntaje_calidad DOUBLE PRECISION, puntaje_asistencia DOUBLE PRECISION,
        puntaje_actitud DOUBLE PRECISION, puntaje_conocimiento DOUBLE PRECISION,
        puntaje_productividad DOUBLE PRECISION, comentarios TEXT,
        estado TEXT DEFAULT 'Borrador',
        creado_en TEXT DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS')));

-- tabla: facturas
CREATE TABLE facturas (
        id              INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
        numero          TEXT    UNIQUE NOT NULL,
        tipo            TEXT    NOT NULL DEFAULT 'Factura',
        numero_pedido   TEXT    DEFAULT '',
        cliente_id      INTEGER,
        cliente_nombre  TEXT    DEFAULT '',
        cliente_nit     TEXT    DEFAULT '',
        empresa         TEXT    NOT NULL DEFAULT 'ANIMUS',
        fecha_emision   TEXT    NOT NULL,
        fecha_vencimiento TEXT  DEFAULT '',
        subtotal        DOUBLE PRECISION    DEFAULT 0,
        descuento       DOUBLE PRECISION    DEFAULT 0,
        iva_pct         DOUBLE PRECISION    DEFAULT 0,
        iva_valor       DOUBLE PRECISION    DEFAULT 0,
        total           DOUBLE PRECISION    DEFAULT 0,
        estado          TEXT    DEFAULT 'Emitida',
        notas           TEXT    DEFAULT '',
        creado_por      TEXT    DEFAULT '',
        fecha_creacion  TEXT    DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS'))
    );

-- tabla: facturas_items
CREATE TABLE facturas_items (
        id              INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
        numero_factura  TEXT    NOT NULL,
        sku             TEXT    DEFAULT '',
        descripcion     TEXT    DEFAULT '',
        cantidad        INTEGER DEFAULT 0,
        precio_unitario DOUBLE PRECISION    DEFAULT 0,
        descuento_pct   DOUBLE PRECISION    DEFAULT 0,
        subtotal        DOUBLE PRECISION    DEFAULT 0
    );

-- tabla: facturas_pagos
CREATE TABLE facturas_pagos (
        id              INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
        numero_factura  TEXT    NOT NULL,
        fecha           TEXT    NOT NULL,
        monto           DOUBLE PRECISION    NOT NULL,
        medio           TEXT    DEFAULT 'Transferencia',
        referencia      TEXT    DEFAULT '',
        registrado_por  TEXT    DEFAULT '',
        fecha_creacion  TEXT    DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS'))
    );

-- tabla: flujo_config
CREATE TABLE flujo_config (
                 clave TEXT PRIMARY KEY, valor TEXT, descripcion TEXT);

-- tabla: flujo_egresos
CREATE TABLE flujo_egresos (
                 id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
                 fecha TEXT, empresa TEXT DEFAULT 'HHA',
                 concepto TEXT NOT NULL, categoria TEXT DEFAULT 'MPs',
                 monto DOUBLE PRECISION DEFAULT 0, periodo TEXT,
                 fuente TEXT DEFAULT 'manual', referencia TEXT DEFAULT '',
                 creado_por TEXT DEFAULT '', observaciones TEXT DEFAULT '');

-- tabla: flujo_ingresos
CREATE TABLE flujo_ingresos (
                 id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
                 fecha TEXT, empresa TEXT DEFAULT 'HHA',
                 concepto TEXT NOT NULL, categoria TEXT DEFAULT 'Ventas',
                 monto DOUBLE PRECISION DEFAULT 0, periodo TEXT,
                 fuente TEXT DEFAULT 'manual', referencia TEXT DEFAULT '',
                 creado_por TEXT DEFAULT '', observaciones TEXT DEFAULT '');

-- tabla: formula_headers
CREATE TABLE formula_headers
                 (id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
                  producto_nombre TEXT UNIQUE, unidad_base_g DOUBLE PRECISION DEFAULT 1000,
                  descripcion TEXT, fecha_creacion TEXT, lote_size_kg DOUBLE PRECISION DEFAULT 0, imagen_url TEXT DEFAULT '', imagen_actualizada_at TEXT, shopify_id TEXT DEFAULT '', shopify_handle TEXT DEFAULT '', descripcion_html TEXT DEFAULT '', descripcion_plain TEXT DEFAULT '', sku_principal TEXT DEFAULT '', precio_venta DOUBLE PRECISION DEFAULT 0, peso_g DOUBLE PRECISION DEFAULT 0, imagenes_extra_json TEXT DEFAULT '[]', shopify_synced_at TEXT, volumen_unitario_ml DOUBLE PRECISION DEFAULT 0, codigo_pt TEXT DEFAULT NULL, activo INTEGER NOT NULL DEFAULT 1, tiene_10ml INTEGER NOT NULL DEFAULT 0, uds_10ml_por_lote INTEGER NOT NULL DEFAULT 0, tipo_10ml TEXT NOT NULL DEFAULT '', tiene_15ml INTEGER NOT NULL DEFAULT 0, uds_15ml_por_lote INTEGER NOT NULL DEFAULT 0);

-- tabla: formula_items
CREATE TABLE formula_items
                 (id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
                  producto_nombre TEXT, material_id TEXT,
                  material_nombre TEXT, porcentaje DOUBLE PRECISION, cantidad_g_por_lote DOUBLE PRECISION DEFAULT 0);

-- tabla: formulas_versiones
CREATE TABLE formulas_versiones (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            formula_id      INTEGER NOT NULL,
            version_num     INTEGER NOT NULL,
            snapshot_json   TEXT NOT NULL,
            motivo_cambio   TEXT DEFAULT '',
            creado_por      TEXT,
            fecha_creacion  TEXT DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS'))
        );

-- tabla: gerencia_inputs
CREATE TABLE gerencia_inputs (
                 id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY, periodo TEXT UNIQUE,
                 saldo_caja DOUBLE PRECISION DEFAULT 0, ingresos_animus DOUBLE PRECISION DEFAULT 0,
                 ingresos_maquila DOUBLE PRECISION DEFAULT 0, notas TEXT DEFAULT '', fecha TEXT);

-- tabla: hallazgos
CREATE TABLE hallazgos (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            codigo TEXT NOT NULL UNIQUE,
            origen TEXT NOT NULL CHECK(origen IN ('INVIMA','BPM_interna','autoinspeccion','auditoria_externa','queja_cliente','otro')),
            titulo TEXT NOT NULL,
            descripcion TEXT,
            area TEXT,
            severidad TEXT NOT NULL DEFAULT 'media' CHECK(severidad IN ('critico','mayor','menor','observacion')),
            fecha_deteccion TEXT NOT NULL DEFAULT (to_char(now(),'YYYY-MM-DD')),
            fecha_limite TEXT,
            fecha_cierre TEXT,
            responsable TEXT,
            accion_propuesta TEXT,
            evidencia_cierre_url TEXT,
            capa_relacionada_id INTEGER,
            estado TEXT NOT NULL DEFAULT 'abierto'
                CHECK(estado IN ('abierto','en_proceso','cerrado','rechazado')),
            creado_por TEXT,
            creado_en TEXT NOT NULL DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS'))
        );

-- tabla: ipc_resultados
CREATE TABLE ipc_resultados (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            ebr_id INTEGER NOT NULL,
            ipc_spec_id INTEGER NOT NULL,
            valor_medido DOUBLE PRECISION DEFAULT NULL,
            valor_texto TEXT DEFAULT '',
            conforme INTEGER DEFAULT NULL,
            medido_por TEXT NOT NULL,
            medido_at_utc TEXT NOT NULL,
            qc_username TEXT DEFAULT '',
            qc_e_sign_id INTEGER DEFAULT NULL,
            notas TEXT DEFAULT '',
            UNIQUE(ebr_id, ipc_spec_id)
        );

-- tabla: ipc_specs
CREATE TABLE ipc_specs (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            mbr_template_id INTEGER NOT NULL,
            mbr_paso_id INTEGER DEFAULT NULL,
            parametro TEXT NOT NULL,
            unidad TEXT NOT NULL DEFAULT '',
            valor_min DOUBLE PRECISION DEFAULT NULL,
            valor_max DOUBLE PRECISION DEFAULT NULL,
            metodo TEXT DEFAULT '',
            obligatorio INTEGER DEFAULT 1,
            notas TEXT DEFAULT ''
        );

-- tabla: limpieza_profunda_calendario
CREATE TABLE limpieza_profunda_calendario (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            fecha TEXT NOT NULL,
            area_codigo TEXT NOT NULL,
            estado TEXT NOT NULL DEFAULT 'programada'
                CHECK(estado IN ('programada','en_proceso','completada','cancelada','reagendada')),
            asignado_a TEXT,
            iniciado_at TEXT,
            terminado_at TEXT,
            iniciado_por TEXT,
            terminado_por TEXT,
            generado_por TEXT NOT NULL DEFAULT 'auto',
            razon_asignacion TEXT,
            notas TEXT,
            creado_en TEXT NOT NULL DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS')),
            UNIQUE(fecha, area_codigo)
        );

-- tabla: maestro_mee
CREATE TABLE maestro_mee (
        codigo TEXT PRIMARY KEY,
        descripcion TEXT NOT NULL,
        categoria TEXT DEFAULT 'Otro',
        proveedor TEXT DEFAULT '',
        fabricante TEXT DEFAULT '',
        estado TEXT DEFAULT 'Activo',
        stock_actual DOUBLE PRECISION DEFAULT 2000,
        stock_minimo DOUBLE PRECISION DEFAULT 1000,
        unidad TEXT DEFAULT 'und',
        fecha_creacion TEXT
    );

-- tabla: maestro_mps
CREATE TABLE maestro_mps
                 (codigo_mp TEXT PRIMARY KEY, nombre_inci TEXT, nombre_comercial TEXT,
                  tipo TEXT, proveedor TEXT, stock_minimo DOUBLE PRECISION DEFAULT 0, activo INTEGER DEFAULT 1, precio_referencia DOUBLE PRECISION DEFAULT 0, unidad_compra TEXT DEFAULT 'kg', lead_time_dias INTEGER DEFAULT 7, ultima_act_precio TEXT DEFAULT '', proveedor_preferido TEXT DEFAULT '', tipo_material TEXT DEFAULT 'MP');

-- tabla: maquila_ingredientes
CREATE TABLE maquila_ingredientes (
                 id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
                 orden_id INTEGER NOT NULL,
                 ingrediente TEXT NOT NULL,
                 porcentaje DOUBLE PRECISION DEFAULT 0,
                 precio_mp_kg DOUBLE PRECISION DEFAULT 0,
                 aporte_kg DOUBLE PRECISION DEFAULT 0);

-- tabla: maquila_ordenes
CREATE TABLE maquila_ordenes (
                 id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
                 numero TEXT UNIQUE,
                 prospecto_id INTEGER,
                 cliente_nombre TEXT NOT NULL,
                 producto TEXT NOT NULL,
                 categoria TEXT DEFAULT '',
                 formato_ml DOUBLE PRECISION DEFAULT 30,
                 lote_kg DOUBLE PRECISION DEFAULT 30,
                 unidades_lote DOUBLE PRECISION DEFAULT 1000,
                 costo_mp_kg DOUBLE PRECISION DEFAULT 0,
                 costo_envase_ud DOUBLE PRECISION DEFAULT 2000,
                 dias_acondicionamiento INTEGER DEFAULT 1,
                 costo_mo_lote DOUBLE PRECISION DEFAULT 0,
                 cf_prorateados DOUBLE PRECISION DEFAULT 0,
                 costo_micro DOUBLE PRECISION DEFAULT 2000000,
                 costo_total_lote DOUBLE PRECISION DEFAULT 0,
                 costo_por_unidad DOUBLE PRECISION DEFAULT 0,
                 margen DOUBLE PRECISION DEFAULT 0.35,
                 precio_ud DOUBLE PRECISION DEFAULT 0,
                 precio_lote DOUBLE PRECISION DEFAULT 0,
                 estado TEXT DEFAULT 'Cotizacion',
                 fecha_orden TEXT,
                 fecha_entrega_est TEXT DEFAULT '',
                 fecha_entrega_real TEXT DEFAULT '',
                 facturado INTEGER DEFAULT 0,
                 monto_facturado DOUBLE PRECISION DEFAULT 0,
                 fecha_factura TEXT DEFAULT '',
                 observaciones TEXT DEFAULT '',
                 creado_por TEXT DEFAULT '',
                 fecha_creacion TEXT DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS')));

-- tabla: maquila_pedidos
CREATE TABLE maquila_pedidos (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            numero TEXT NOT NULL UNIQUE,
            cliente_id INTEGER NOT NULL,
            cliente_nombre TEXT,
            producto_nombre TEXT NOT NULL,
            presentacion_id INTEGER,
            unidades INTEGER NOT NULL,
            kg_estimados DOUBLE PRECISION,
            fecha_pedido TEXT NOT NULL DEFAULT (to_char(now(),'YYYY-MM-DD')),
            fecha_entrega_objetivo TEXT,
            estado TEXT NOT NULL DEFAULT 'recibido'
                CHECK(estado IN ('recibido','planificado','en_produccion','listo_entrega','entregado','cancelado')),
            produccion_id INTEGER,
            precio_unidad DOUBLE PRECISION,
            valor_total DOUBLE PRECISION,
            observaciones TEXT,
            creado_por TEXT,
            creado_en TEXT NOT NULL DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS')),
            actualizado_en TEXT
        );

-- tabla: maquila_pipeline
CREATE TABLE maquila_pipeline (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            empresa TEXT NOT NULL,
            contacto_nombre TEXT,
            contacto_email TEXT,
            contacto_telefono TEXT,
            origen TEXT,
            stage TEXT NOT NULL DEFAULT 'consulta'
                CHECK(stage IN ('consulta','nda','brief','cotizacion','contrato','produccion','ganado','perdido')),
            valor_estimado_cop DOUBLE PRECISION DEFAULT 0,
            volumen_estimado_unds INTEGER DEFAULT 0,
            producto_descripcion TEXT,
            nda_firmado_at TEXT,
            brief_recibido_at TEXT,
            cotizacion_enviada_at TEXT,
            contrato_firmado_at TEXT,
            fecha_cierre_estimada TEXT,
            owner TEXT,
            notas TEXT,
            motivo_perdida TEXT,
            creado_en TEXT NOT NULL DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS')),
            actualizado_en TEXT
        );

-- tabla: maquila_prospectos
CREATE TABLE maquila_prospectos (
                 id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
                 numero_brief TEXT DEFAULT '',
                 fecha TEXT, kam TEXT DEFAULT '',
                 empresa TEXT NOT NULL, contacto TEXT DEFAULT '',
                 cargo TEXT DEFAULT '', whatsapp TEXT DEFAULT '',
                 email TEXT DEFAULT '', ciudad TEXT DEFAULT '',
                 canal_origen TEXT DEFAULT '',
                 categoria_producto TEXT DEFAULT '',
                 descripcion_producto TEXT DEFAULT '',
                 claims TEXT DEFAULT '',
                 restricciones TEXT DEFAULT '',
                 estado_formula TEXT DEFAULT 'Sin formular',
                 volumen_lote TEXT DEFAULT '',
                 frecuencia TEXT DEFAULT '',
                 empaque TEXT DEFAULT '',
                 mercado TEXT DEFAULT 'Nacional',
                 nso TEXT DEFAULT 'No tiene',
                 presupuesto TEXT DEFAULT '',
                 etapa TEXT DEFAULT 'Contacto',
                 nivel_recomendado TEXT DEFAULT '',
                 viabilidad TEXT DEFAULT '',
                 riesgos TEXT DEFAULT '',
                 valor_estimado_lote DOUBLE PRECISION DEFAULT 0,
                 ticket_mes DOUBLE PRECISION DEFAULT 0,
                 proxima_accion TEXT DEFAULT '',
                 observaciones TEXT DEFAULT '',
                 estado TEXT DEFAULT 'Activo',
                 creado_por TEXT DEFAULT '',
                 fecha_creacion TEXT DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS')), es_incubacion INTEGER DEFAULT 0, nivel_servicio TEXT DEFAULT '', kam_asignado TEXT DEFAULT 'Luz', contacto_referido TEXT DEFAULT '');

-- tabla: marketing_agentes_feedback
CREATE TABLE marketing_agentes_feedback (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            log_id        INTEGER NOT NULL,
            agente        TEXT NOT NULL,
            feedback      TEXT NOT NULL CHECK(feedback IN ('util','no_util','ejecutado')),
            comentario    TEXT,
            usuario       TEXT,
            fecha         TEXT DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS'))
        );

-- tabla: marketing_agentes_log
CREATE TABLE marketing_agentes_log (
        id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
        agente TEXT NOT NULL,
        accion TEXT,
        resultado TEXT,
        fecha TEXT DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS')),
        ejecutado_por TEXT
    );

-- tabla: marketing_alertas_enviadas
CREATE TABLE marketing_alertas_enviadas (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            agente TEXT NOT NULL,
            sku TEXT,
            tipo_alerta TEXT,
            severidad TEXT,
            mensaje TEXT,
            destinatarios TEXT,
            fecha_envio TEXT NOT NULL DEFAULT (to_char(now(),'YYYY-MM-DD')),
            enviado_at TEXT DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS'))
        );

-- tabla: marketing_campana_influencer
CREATE TABLE marketing_campana_influencer (
        id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
        campana_id INTEGER NOT NULL,
        influencer_id INTEGER NOT NULL,
        monto_pactado DOUBLE PRECISION DEFAULT 0,
        monto_pagado DOUBLE PRECISION DEFAULT 0,
        fecha_pago TEXT,
        alcance_real INTEGER DEFAULT 0,
        impresiones INTEGER DEFAULT 0,
        clicks INTEGER DEFAULT 0,
        conversiones INTEGER DEFAULT 0,
        estado TEXT DEFAULT 'Pendiente',
        notas TEXT
    );

-- tabla: marketing_campanas
CREATE TABLE marketing_campanas (
        id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
        nombre TEXT NOT NULL,
        tipo TEXT DEFAULT 'Digital',
        estado TEXT DEFAULT 'Planificada',
        presupuesto DOUBLE PRECISION DEFAULT 0,
        presupuesto_gastado DOUBLE PRECISION DEFAULT 0,
        fecha_inicio TEXT,
        fecha_fin TEXT,
        sku_objetivo TEXT,
        objetivo_unidades INTEGER DEFAULT 0,
        resultado_unidades INTEGER DEFAULT 0,
        resultado_ventas DOUBLE PRECISION DEFAULT 0,
        canal TEXT,
        notas TEXT,
        creada_por TEXT,
        fecha_creacion TEXT DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS'))
    );

-- tabla: marketing_contenido
CREATE TABLE marketing_contenido (
        id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
        campana_id INTEGER,
        influencer_id INTEGER,
        tipo TEXT DEFAULT 'Post',
        plataforma TEXT DEFAULT 'Instagram',
        fecha_publicacion TEXT,
        estado TEXT DEFAULT 'Borrador',
        caption TEXT,
        url_publicacion TEXT,
        likes INTEGER DEFAULT 0,
        comentarios INTEGER DEFAULT 0,
        shares INTEGER DEFAULT 0,
        guardados INTEGER DEFAULT 0,
        alcance INTEGER DEFAULT 0,
        impresiones INTEGER DEFAULT 0,
        clicks INTEGER DEFAULT 0,
        conversiones INTEGER DEFAULT 0,
        notas TEXT,
        creado_por TEXT,
        fecha_creacion TEXT DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS'))
    , sku_objetivo TEXT DEFAULT '', mensaje_principal TEXT DEFAULT '', fecha_programada TEXT DEFAULT '');

-- tabla: marketing_influencers
CREATE TABLE marketing_influencers (
        id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
        nombre TEXT NOT NULL,
        red_social TEXT DEFAULT 'Instagram',
        usuario_red TEXT,
        seguidores INTEGER DEFAULT 0,
        engagement_rate DOUBLE PRECISION DEFAULT 0,
        nicho TEXT,
        tarifa DOUBLE PRECISION DEFAULT 0,
        estado TEXT DEFAULT 'Activo',
        email TEXT,
        telefono TEXT,
        notas TEXT,
        fecha_registro TEXT DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS'))
    , banco TEXT DEFAULT '', cuenta_bancaria TEXT DEFAULT '', cedula_nit TEXT DEFAULT '', tipo_cuenta TEXT DEFAULT 'Ahorros', motivo_baja TEXT DEFAULT '', fecha_baja TEXT DEFAULT '', ciudad TEXT DEFAULT '', instagram TEXT DEFAULT '', tipo TEXT DEFAULT '', discount_code TEXT DEFAULT '', ciclo_pago TEXT DEFAULT 'Mensual');

-- tabla: marketing_influencers_metrics
CREATE TABLE marketing_influencers_metrics (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            influencer_id INTEGER NOT NULL,
            fecha TEXT NOT NULL,
            seguidores INTEGER,
            siguiendo INTEGER,
            posts_total INTEGER,
            engagement_rate DOUBLE PRECISION,
            avg_likes INTEGER,
            avg_comments INTEGER,
            rank_global INTEGER,
            grade TEXT,
            fuente TEXT NOT NULL,
            raw_json TEXT,
            creado_en TEXT DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS')),
            UNIQUE(influencer_id, fecha, fuente)
        );

-- tabla: marketing_push_alerts_log
CREATE TABLE marketing_push_alerts_log (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            tipo          TEXT NOT NULL,
            clave_unica   TEXT NOT NULL,
            destinatario  TEXT NOT NULL,
            asunto        TEXT,
            cuerpo_resumen TEXT,
            severidad     TEXT DEFAULT 'medio',
            fecha         TEXT DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS')),
            UNIQUE(tipo, clave_unica, destinatario, fecha)
        );

-- tabla: mbr_pasos
CREATE TABLE mbr_pasos (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            mbr_template_id INTEGER NOT NULL,
            orden INTEGER NOT NULL,
            fase TEXT DEFAULT '',
            descripcion TEXT NOT NULL,
            tipo_paso TEXT DEFAULT 'otro',
            equipo_requerido TEXT DEFAULT '',
            tiempo_estimado_min INTEGER DEFAULT 0,
            requiere_e_sign INTEGER DEFAULT 0,
            requiere_qc INTEGER DEFAULT 0,
            notas TEXT DEFAULT '',
            UNIQUE(mbr_template_id, orden)
        );

-- tabla: mbr_templates
CREATE TABLE mbr_templates (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            producto_nombre TEXT NOT NULL,
            formula_version_id INTEGER,
            version INTEGER NOT NULL,
            estado TEXT NOT NULL DEFAULT 'draft',
            titulo TEXT DEFAULT '',
            descripcion TEXT DEFAULT '',
            lote_size_g DOUBLE PRECISION NOT NULL,
            tiempo_total_estimado_min INTEGER DEFAULT 0,
            creado_por TEXT NOT NULL,
            creado_at_utc TEXT DEFAULT (to_char((now() AT TIME ZONE 'UTC'),'YYYY-MM-DD HH24:MI:SS')),
            updated_at_utc TEXT DEFAULT (to_char((now() AT TIME ZONE 'UTC'),'YYYY-MM-DD HH24:MI:SS')),
            aprobado_por TEXT DEFAULT '',
            aprobado_at_utc TEXT DEFAULT NULL,
            aprobado_signature_id INTEGER DEFAULT NULL,
            obsoleto_at_utc TEXT DEFAULT NULL,
            obsoleto_motivo TEXT DEFAULT '',
            UNIQUE(producto_nombre, version)
        );

-- tabla: mee_lead_time_config
CREATE TABLE mee_lead_time_config (
            mee_codigo TEXT PRIMARY KEY,
            proveedor_principal TEXT NOT NULL DEFAULT '',
            origen TEXT NOT NULL DEFAULT 'Local'
                CHECK(origen IN ('China','Local','Mixto')),
            lead_time_dias INTEGER NOT NULL DEFAULT 30,
            moq_unidades INTEGER NOT NULL DEFAULT 0,
            precio_unit DOUBLE PRECISION NOT NULL DEFAULT 0,
            disparo_d20 INTEGER NOT NULL DEFAULT 0,
            aplica INTEGER NOT NULL DEFAULT 1,
            notas TEXT,
            actualizado_en TEXT,
            actualizado_por TEXT
        , disparo_post_envasado INTEGER NOT NULL DEFAULT 0);

-- tabla: mensajes_internos
CREATE TABLE mensajes_internos (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            de_usuario          TEXT NOT NULL,
            a_usuario           TEXT NOT NULL,
            asunto              TEXT DEFAULT '',
            mensaje             TEXT NOT NULL,
            fecha               TEXT DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS')),
            leido_at            TEXT,
            relacionado_tarea_id INTEGER
        );

-- tabla: movimientos
CREATE TABLE movimientos
                 (id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
                  material_id TEXT, material_nombre TEXT, cantidad DOUBLE PRECISION,
                  tipo TEXT, fecha TEXT, observaciones TEXT,
                  lote TEXT, fecha_vencimiento TEXT, estanteria TEXT,
                  posicion TEXT, proveedor TEXT, estado_lote TEXT, operador TEXT, precio_kg DOUBLE PRECISION DEFAULT 0, numero_factura TEXT DEFAULT '', numero_oc TEXT DEFAULT '', valor_total DOUBLE PRECISION DEFAULT 0, zona TEXT DEFAULT 'Almacen');

-- tabla: movimientos_mee
CREATE TABLE movimientos_mee (
        id          INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
        mee_codigo  TEXT     NOT NULL,
        tipo        TEXT     NOT NULL CHECK(tipo IN ('Entrada','Salida','Ajuste')),
        cantidad    DOUBLE PRECISION     NOT NULL,
        unidad      TEXT     DEFAULT 'und',
        lote_ref    TEXT     DEFAULT '',
        batch_ref   TEXT     DEFAULT '',
        responsable TEXT     DEFAULT '',
        observaciones TEXT   DEFAULT '',
        fecha       TEXT DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS')),
        anulado     INTEGER  DEFAULT 0
    );

-- tabla: mp_alcanza_snapshots
CREATE TABLE mp_alcanza_snapshots (
            fecha TEXT PRIMARY KEY,
            total_mps INTEGER,
            comprar_ya_total INTEGER,
            comprar_1_2_sem_total INTEGER,
            comprar_1_mes_total INTEGER,
            ok_total INTEGER,
            sin_uso_total INTEGER,
            comprar_ya_codigos TEXT,
            creado_en TEXT NOT NULL DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS')),
            origen TEXT DEFAULT 'cron'
        );

-- tabla: mp_formula_bridge
CREATE TABLE mp_formula_bridge (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            formula_material_id TEXT NOT NULL,
            formula_material_nombre TEXT,
            bodega_material_id TEXT NOT NULL,
            bodega_material_nombre TEXT,
            bodega_inci TEXT,
            notas TEXT,
            activo INTEGER DEFAULT 1,
            creado_en TEXT DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS')),
            UNIQUE(formula_material_id, bodega_material_id)
        );

-- tabla: mp_lead_time_config
CREATE TABLE mp_lead_time_config (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            material_id TEXT NOT NULL,
            material_nombre TEXT,
            proveedor_principal TEXT,
            lead_time_dias INTEGER NOT NULL DEFAULT 14,
            buffer_dias INTEGER NOT NULL DEFAULT 30,
            cobertura_min_dias INTEGER NOT NULL DEFAULT 30,
            cobertura_ideal_dias INTEGER NOT NULL DEFAULT 60,
            origen TEXT NOT NULL DEFAULT 'local'
                CHECK(origen IN ('local','nacional','china','usa','europa','otro')),
            es_envase INTEGER NOT NULL DEFAULT 0,
            activo INTEGER NOT NULL DEFAULT 1,
            actualizado_en TEXT,
            UNIQUE(material_id)
        );

-- tabla: no_conformidades
CREATE TABLE no_conformidades (
                 id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
                 fecha TEXT NOT NULL,
                 tipo TEXT DEFAULT 'MP',
                 descripcion TEXT NOT NULL,
                 area TEXT DEFAULT 'Produccion',
                 responsable TEXT DEFAULT '',
                 lote TEXT DEFAULT '',
                 codigo_mp TEXT DEFAULT '',
                 impacto TEXT DEFAULT 'Menor',
                 accion_correctiva TEXT DEFAULT '',
                 estado TEXT DEFAULT 'Abierta',
                 fecha_cierre TEXT DEFAULT '',
                 cerrado_por TEXT DEFAULT '',
                 creado_por TEXT DEFAULT '');

-- tabla: nomina_registros
CREATE TABLE nomina_registros (
        id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
        periodo TEXT, empleado_id INTEGER, salario_base DOUBLE PRECISION,
        dias_trabajados INTEGER DEFAULT 30, horas_extras DOUBLE PRECISION DEFAULT 0,
        valor_horas_extras DOUBLE PRECISION DEFAULT 0, subsidio_transporte DOUBLE PRECISION DEFAULT 0,
        bonificaciones DOUBLE PRECISION DEFAULT 0, descuento_salud DOUBLE PRECISION DEFAULT 0,
        descuento_pension DOUBLE PRECISION DEFAULT 0, otros_descuentos DOUBLE PRECISION DEFAULT 0,
        salario_neto DOUBLE PRECISION DEFAULT 0, estado TEXT DEFAULT 'Generada', aprobado_por TEXT DEFAULT NULL, aprobado_en TEXT DEFAULT NULL, pagado_por TEXT DEFAULT NULL, pagado_en TEXT DEFAULT NULL,
        UNIQUE(periodo,empleado_id));

-- tabla: notificaciones_app
CREATE TABLE notificaciones_app (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            destinatario TEXT NOT NULL,
            tipo TEXT NOT NULL,
            titulo TEXT NOT NULL,
            body TEXT,
            link TEXT,
            remitente TEXT,
            importante INTEGER NOT NULL DEFAULT 0,
            leido_at TEXT,
            creado_en TEXT NOT NULL DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS'))
        );

-- tabla: notificaciones_empleados
CREATE TABLE notificaciones_empleados (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            empleado_username TEXT NOT NULL,
            empleado_nombre TEXT,
            tipo TEXT NOT NULL CHECK(tipo IN
                ('salud','permiso','cita_medica','enfermedad','licencia','otro')),
            asunto TEXT NOT NULL,
            descripcion TEXT,
            fecha_inicio TEXT,
            fecha_fin TEXT,
            adjunto_url TEXT,
            estado TEXT NOT NULL DEFAULT 'pendiente'
                CHECK(estado IN ('pendiente','aprobada','rechazada','vista')),
            notificado_a TEXT,
            comentario_jefe TEXT,
            resuelto_por TEXT,
            resuelto_en TEXT,
            creado_en TEXT NOT NULL DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS'))
        );

-- tabla: op_counters
CREATE TABLE op_counters (
            year INTEGER PRIMARY KEY,
            counter INTEGER NOT NULL DEFAULT 0,
            updated_at_utc TEXT NOT NULL DEFAULT (to_char((now() AT TIME ZONE 'UTC'),'YYYY-MM-DD HH24:MI:SS'))
        );

-- tabla: operarios_fija_audit
CREATE TABLE operarios_fija_audit (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            operario_id INTEGER NOT NULL,
            valor_anterior INTEGER NOT NULL,
            valor_nuevo INTEGER NOT NULL,
            cambiado_at TEXT NOT NULL DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS'))
        );

-- tabla: operarios_planta
CREATE TABLE operarios_planta (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            nombre TEXT NOT NULL,
            apellido TEXT,
            rol_predeterminado TEXT,
            fija_en_dispensacion INTEGER NOT NULL DEFAULT 0,
            es_jefe_produccion INTEGER NOT NULL DEFAULT 0,
            activo INTEGER NOT NULL DEFAULT 1,
            creado_en TEXT DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS'))
        );

-- tabla: ordenes_compra
CREATE TABLE ordenes_compra
                 (id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
                  numero_oc TEXT UNIQUE, fecha TEXT, estado TEXT DEFAULT 'Borrador',
                  proveedor TEXT, valor_total DOUBLE PRECISION DEFAULT 0,
                  observaciones TEXT, creado_por TEXT, fecha_entrega_est TEXT, categoria TEXT DEFAULT 'MP', remision_code TEXT DEFAULT '', autorizado_por TEXT DEFAULT '', fecha_autorizacion TEXT DEFAULT '', pagado_por TEXT DEFAULT '', fecha_pago TEXT DEFAULT '', fecha_recepcion TEXT DEFAULT '', observaciones_recepcion TEXT DEFAULT '', tiene_discrepancias INTEGER DEFAULT 0, con_iva INTEGER DEFAULT 0, valor_sin_iva DOUBLE PRECISION DEFAULT 0, comprobante_imagen TEXT DEFAULT '', medio_pago TEXT DEFAULT '', recibido_por TEXT DEFAULT '', numero_factura_proveedor TEXT DEFAULT '', centro_costos TEXT DEFAULT 'general');

-- tabla: ordenes_compra_items
CREATE TABLE ordenes_compra_items
                 (id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
                  numero_oc TEXT, codigo_mp TEXT, nombre_mp TEXT,
                  cantidad_g DOUBLE PRECISION, precio_unitario DOUBLE PRECISION DEFAULT 0, subtotal DOUBLE PRECISION DEFAULT 0, estado_recepcion TEXT DEFAULT 'OK', notas_recepcion TEXT DEFAULT '', precio_unitario_real DOUBLE PRECISION DEFAULT 0, cantidad_recibida_g DOUBLE PRECISION DEFAULT 0, lote_asignado TEXT DEFAULT '');

-- tabla: pagos_influencers
CREATE TABLE pagos_influencers (
    id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    influencer_id INTEGER,
    influencer_nombre TEXT NOT NULL,
    valor INTEGER NOT NULL DEFAULT 0,
    fecha TEXT NOT NULL,
    estado TEXT DEFAULT 'Pendiente',
    concepto TEXT DEFAULT '',
    numero_oc TEXT DEFAULT '',
    created_at TEXT DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS'))
, fecha_publicacion TEXT DEFAULT '', entregable TEXT DEFAULT '');

-- tabla: pagos_oc
CREATE TABLE pagos_oc (
            id                   INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            numero_oc            TEXT NOT NULL,
            monto                DOUBLE PRECISION NOT NULL,
            medio                TEXT DEFAULT 'Transferencia',
            fecha_pago           TEXT NOT NULL DEFAULT (to_char((now() AT TIME ZONE 'UTC'),'YYYY-MM-DD HH24:MI:SS')),
            registrado_por       TEXT NOT NULL DEFAULT '',
            numero_factura_proveedor TEXT DEFAULT '',
            comprobante_imagen   TEXT DEFAULT '',
            observaciones        TEXT DEFAULT ''
        );

-- tabla: pedidos
CREATE TABLE pedidos (
                 id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
                 numero TEXT UNIQUE, cliente_id INTEGER REFERENCES clientes(id),
                 fecha TEXT, fecha_entrega_est TEXT, estado TEXT DEFAULT 'Confirmado',
                 empresa TEXT DEFAULT 'ANIMUS', valor_total DOUBLE PRECISION DEFAULT 0,
                 observaciones TEXT DEFAULT '', creado_por TEXT DEFAULT '',
                 fecha_despacho TEXT DEFAULT '', numero_factura TEXT DEFAULT '', monto_pagado DOUBLE PRECISION DEFAULT 0, estado_pago TEXT DEFAULT 'Pendiente', canal_venta TEXT DEFAULT 'Directo', descuento_total_cop DOUBLE PRECISION DEFAULT 0);

-- tabla: pedidos_b2b
CREATE TABLE pedidos_b2b (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            cliente_id TEXT NOT NULL,
            cliente_nombre TEXT NOT NULL,
            producto_nombre TEXT NOT NULL,
            cantidad_uds INTEGER NOT NULL,
            ml_unidad DOUBLE PRECISION NOT NULL DEFAULT 30,
            fecha_estimada TEXT,
            estado TEXT NOT NULL DEFAULT 'pendiente'
                CHECK(estado IN ('pendiente','confirmado','en_produccion','despachado','cancelado')),
            notas TEXT DEFAULT '',
            creado_por TEXT NOT NULL,
            creado_at_utc TEXT NOT NULL DEFAULT (to_char((now() AT TIME ZONE 'UTC'),'YYYY-MM-DD HH24:MI:SS')),
            actualizado_at_utc TEXT NOT NULL DEFAULT (to_char((now() AT TIME ZONE 'UTC'),'YYYY-MM-DD HH24:MI:SS'))
        );

-- tabla: pedidos_items
CREATE TABLE pedidos_items (
                 id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
                 numero_pedido TEXT, sku TEXT, descripcion TEXT,
                 cantidad INTEGER DEFAULT 0, precio_unitario DOUBLE PRECISION DEFAULT 0,
                 subtotal DOUBLE PRECISION DEFAULT 0, lote_pt TEXT DEFAULT '');

-- tabla: precio_historico_mp
CREATE TABLE precio_historico_mp (
            id              INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            codigo_mp       TEXT NOT NULL,
            nombre_mp       TEXT DEFAULT '',
            proveedor       TEXT DEFAULT '',
            precio_unit_g   DOUBLE PRECISION NOT NULL,
            cantidad_g      DOUBLE PRECISION DEFAULT 0,
            valor_total     DOUBLE PRECISION DEFAULT 0,
            fecha           TEXT NOT NULL DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS')),
            fuente          TEXT DEFAULT 'manual'
                CHECK(fuente IN ('manual','sol_editada','oc_creada','oc_pagada','recepcion','import')),
            sol_numero      TEXT DEFAULT '',
            oc_numero       TEXT DEFAULT '',
            usuario         TEXT DEFAULT '',
            observaciones   TEXT DEFAULT ''
        );

-- tabla: precios_mp_historico
CREATE TABLE precios_mp_historico (
                 id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
                 codigo_mp TEXT NOT NULL, proveedor TEXT NOT NULL,
                 precio_kg DOUBLE PRECISION NOT NULL, fecha TEXT NOT NULL,
                 numero_oc TEXT DEFAULT '', numero_factura TEXT DEFAULT '',
                 origen TEXT DEFAULT 'ingreso', observaciones TEXT DEFAULT '');

-- tabla: produccion_checklist
CREATE TABLE produccion_checklist (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            produccion_id      INTEGER,
            produccion_ref     TEXT,
            producto_nombre    TEXT NOT NULL,
            fecha_planeada     TEXT NOT NULL,
            cantidad_kg        DOUBLE PRECISION DEFAULT 0,
            item_tipo          TEXT NOT NULL,
            descripcion        TEXT NOT NULL,
            cantidad_requerida DOUBLE PRECISION DEFAULT 0,
            unidad             TEXT DEFAULT 'g',
            codigo_mp          TEXT DEFAULT '',
            stock_actual       DOUBLE PRECISION DEFAULT 0,
            deficit            DOUBLE PRECISION DEFAULT 0,
            estado             TEXT DEFAULT 'pendiente'
                CHECK(estado IN ('pendiente','verificado_ok','solicitado',
                                 'en_transito','recibido','listo','no_aplica')),
            proveedor          TEXT DEFAULT '',
            solicitud_numero   TEXT DEFAULT '',
            oc_numero          TEXT DEFAULT '',
            fecha_solicitud    TEXT,
            fecha_eta          TEXT,
            fecha_recibido     TEXT,
            responsable        TEXT DEFAULT '',
            observaciones      TEXT DEFAULT '',
            dias_anticipacion  INTEGER DEFAULT 30,
            actualizado_at     TEXT DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS')),
            actualizado_por    TEXT,
            fecha_creacion     TEXT DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS'))
        , mee_codigo_asignado TEXT DEFAULT '', decoracion_tipo TEXT DEFAULT '', cantidad_unidades DOUBLE PRECISION DEFAULT 0, solicitud_produccion_id INTEGER, consumido_at TEXT, consumido_por TEXT DEFAULT '', cantidad_consumida_real DOUBLE PRECISION DEFAULT 0, consumido_contexto TEXT DEFAULT '');

-- tabla: produccion_envasado
CREATE TABLE produccion_envasado (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            produccion_id INTEGER NOT NULL,
            producto_nombre TEXT NOT NULL,
            lote TEXT NOT NULL,
            presentacion_id INTEGER,
            presentacion_etiqueta TEXT,
            unidades_planeadas INTEGER,
            unidades_envasadas INTEGER,
            envase_codigo TEXT,
            iniciado_at TEXT NOT NULL DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS')),
            iniciado_por TEXT,
            terminado_at TEXT,
            terminado_por TEXT,
            estado TEXT NOT NULL DEFAULT 'en_proceso'
                CHECK(estado IN ('en_proceso','terminado','cancelado')),
            muestra_micro_id INTEGER,
            notas TEXT
        );

-- tabla: produccion_programada
CREATE TABLE produccion_programada (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            producto TEXT NOT NULL,
            fecha_programada TEXT NOT NULL,
            lotes INTEGER DEFAULT 1,
            estado TEXT DEFAULT 'pendiente',
            observaciones TEXT,
            creado_en TEXT DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS')),
            gcal_event_id TEXT
        , origen TEXT DEFAULT 'manual', cantidad_kg DOUBLE PRECISION DEFAULT 0, inventario_descontado_at TEXT, area_id INTEGER, operario_dispensacion_id INTEGER, operario_elaboracion_id INTEGER, operario_envasado_id INTEGER, operario_acondicionamiento_id INTEGER, inicio_real_at TEXT, fin_real_at TEXT, area_envasado_id INTEGER, bloqueado_at TEXT, bloqueado_por TEXT DEFAULT '', semana_workflow_id TEXT DEFAULT '', kg_real DOUBLE PRECISION DEFAULT NULL, unidades_real INTEGER DEFAULT NULL, merma_pct DOUBLE PRECISION DEFAULT NULL, motivo_pausa TEXT DEFAULT NULL, pausado_at TEXT DEFAULT NULL, pausado_por TEXT DEFAULT NULL);

-- tabla: producciones
CREATE TABLE producciones
                 (id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
                  producto TEXT, cantidad DOUBLE PRECISION, fecha TEXT, estado TEXT, observaciones TEXT, operador TEXT, lote TEXT DEFAULT '', presentacion TEXT DEFAULT '', sop_referencia TEXT DEFAULT '', sop_version TEXT DEFAULT '', formula_snapshot_json TEXT);

-- tabla: producto_canonico_config
CREATE TABLE producto_canonico_config (
            producto_nombre TEXT PRIMARY KEY,
            kg_por_lote DOUBLE PRECISION DEFAULT 0,
            ml_unidad INTEGER DEFAULT 30,
            frecuencia_dias INTEGER DEFAULT 0,
            activo INTEGER DEFAULT 1,
            actualizado_at TEXT,
            actualizado_por TEXT,
            notas TEXT DEFAULT ''
        );

-- tabla: producto_perfil_riesgo
CREATE TABLE producto_perfil_riesgo (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            producto_nombre TEXT NOT NULL UNIQUE,
            tiene_pigmento INTEGER NOT NULL DEFAULT 0,
            color_descripcion TEXT,
            es_acido INTEGER NOT NULL DEFAULT 0,
            requiere_asepsia_extra INTEGER NOT NULL DEFAULT 0,
            riesgo_arrastre_pct INTEGER NOT NULL DEFAULT 5,
            notas TEXT,
            actualizado_en TEXT
        );

-- tabla: producto_presentaciones
CREATE TABLE producto_presentaciones (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            producto_nombre TEXT NOT NULL,
            categoria TEXT,
            presentacion_codigo TEXT NOT NULL,
            etiqueta TEXT NOT NULL,
            volumen_ml DOUBLE PRECISION,
            peso_g DOUBLE PRECISION,
            envase_codigo TEXT,
            factor_g_por_unidad DOUBLE PRECISION,
            sku_shopify TEXT,
            es_default INTEGER NOT NULL DEFAULT 0,
            activo INTEGER NOT NULL DEFAULT 1,
            notas TEXT,
            creado_en TEXT NOT NULL DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS')),
            actualizado_en TEXT,
            UNIQUE(producto_nombre, presentacion_codigo)
        );

-- tabla: proveedores
CREATE TABLE proveedores
                 (id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
                  nombre TEXT UNIQUE, contacto TEXT, email TEXT, telefono TEXT,
                  categoria TEXT, condiciones_pago TEXT, activo INTEGER DEFAULT 1,
                  fecha_creacion TEXT, nit TEXT, id_interno TEXT, direccion TEXT, num_cuenta TEXT, tipo_cuenta TEXT, banco TEXT, cert_bancario TEXT, estado_lpa TEXT, ultima_evaluacion TEXT, vencimiento_docs TEXT, acuerdo_calidad TEXT, rut INTEGER DEFAULT 0, camara_comercio INTEGER DEFAULT 0, concepto_compra TEXT, motivo_baja TEXT, fecha_baja TEXT);

-- tabla: quejas_clientes
CREATE TABLE quejas_clientes (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            codigo TEXT UNIQUE,
            fecha_recepcion TEXT NOT NULL DEFAULT (to_char(now(),'YYYY-MM-DD')),
            recibido_por TEXT NOT NULL,
            canal TEXT NOT NULL DEFAULT 'otro'
              CHECK(canal IN ('email','telefono','whatsapp','redes_sociales',
                              'presencial','distribuidor','formulario_web','otro')),
            cliente_nombre TEXT NOT NULL,
            cliente_contacto TEXT,
            cliente_tipo TEXT
              CHECK(cliente_tipo IN ('consumidor_final','distribuidor','retail','medico','otro')
                    OR cliente_tipo IS NULL),
            producto TEXT,
            lote TEXT,
            fecha_compra TEXT,
            establecimiento_compra TEXT,
            tipo_queja TEXT NOT NULL DEFAULT 'otro'
              CHECK(tipo_queja IN ('reaccion_adversa','calidad_producto','envase_empaque',
                                    'cantidad_volumen','fecha_vencimiento',
                                    'sabor_olor_textura','eficacia','documentacion',
                                    'servicio','otro')),
            descripcion TEXT NOT NULL,
            impacto_salud INTEGER NOT NULL DEFAULT 0,
            severidad TEXT
              CHECK(severidad IN ('critica','mayor','menor','informativa') OR severidad IS NULL),
            triaje_descripcion TEXT,
            triaje_por TEXT,
            triaje_at TEXT,
            requiere_desviacion INTEGER NOT NULL DEFAULT 0,
            desviacion_id INTEGER,
            requiere_recall INTEGER NOT NULL DEFAULT 0,
            causa_raiz TEXT,
            investigacion_por TEXT,
            investigacion_at TEXT,
            respuesta_descripcion TEXT,
            respuesta_canal TEXT
              CHECK(respuesta_canal IN ('email','telefono','whatsapp','presencial',
                                          'carta','formulario_web','otro')
                    OR respuesta_canal IS NULL),
            respondido_por TEXT,
            respondido_at TEXT,
            fecha_compromiso TEXT,
            cliente_satisfecho INTEGER,
            accion_correctiva TEXT,
            cerrado_por TEXT,
            fecha_cierre TEXT,
            observaciones_cierre TEXT,
            estado TEXT NOT NULL DEFAULT 'nueva'
              CHECK(estado IN ('nueva','en_triaje','en_investigacion',
                                'respondida','cerrada','rechazada')),
            creado_en TEXT NOT NULL DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS')),
            actualizado_en TEXT
        );

-- tabla: quejas_clientes_eventos
CREATE TABLE quejas_clientes_eventos (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            queja_id INTEGER NOT NULL,
            evento_tipo TEXT NOT NULL,
            estado_anterior TEXT,
            estado_nuevo TEXT,
            usuario TEXT,
            comentario TEXT,
            creado_en TEXT NOT NULL DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS'))
        );

-- tabla: quejas_internas
CREATE TABLE quejas_internas (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            de_usuario          TEXT NOT NULL,
            contexto            TEXT NOT NULL,
            severidad_ia        TEXT,
            analisis_ia         TEXT,
            accion_sugerida_ia  TEXT,
            escalada_a          TEXT,
            estado              TEXT DEFAULT 'Pendiente' CHECK(estado IN ('Pendiente','Analizada','Escalada','Resuelta','Descartada')),
            fecha               TEXT DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS')),
            fecha_resolucion    TEXT,
            resolucion          TEXT
        );

-- tabla: rate_limit
CREATE TABLE rate_limit (
        ip   TEXT PRIMARY KEY,
        attempts   INTEGER DEFAULT 0,
        locked_until DOUBLE PRECISION DEFAULT 0,
        last_attempt DOUBLE PRECISION DEFAULT 0
    );

-- tabla: recall_log
CREATE TABLE recall_log (
        id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
        lote_pt TEXT NOT NULL,
        sku TEXT, motivo TEXT,
        total_despachos INTEGER DEFAULT 0,
        total_unidades INTEGER DEFAULT 0,
        fecha_recall TEXT DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS')),
        ejecutado_por TEXT,
        estado TEXT DEFAULT 'Simulacion'
    );

-- tabla: recalls
CREATE TABLE recalls (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            codigo TEXT UNIQUE,
            fecha_inicio TEXT NOT NULL DEFAULT (to_char(now(),'YYYY-MM-DD')),
            iniciado_por TEXT NOT NULL,
            origen TEXT NOT NULL DEFAULT 'otro'
              CHECK(origen IN ('desviacion','queja_cliente','hallazgo_interno',
                                'auditoria','reaccion_adversa','invima','otro')),
            origen_referencia TEXT,
            desviacion_id INTEGER,
            queja_id INTEGER,
            producto TEXT NOT NULL,
            lotes_afectados TEXT NOT NULL,
            cantidad_fabricada INTEGER,
            cantidad_distribuida INTEGER,
            motivo TEXT NOT NULL,
            riesgo_descripcion TEXT,
            clase_recall TEXT
              CHECK(clase_recall IN ('clase_I','clase_II','clase_III') OR clase_recall IS NULL),
            alcance_geografico TEXT
              CHECK(alcance_geografico IN ('local','regional','nacional','internacional')
                    OR alcance_geografico IS NULL),
            clasificado_por TEXT,
            clasificado_at TEXT,
            justificacion_clasificacion TEXT,
            notificacion_invima_at TEXT,
            notificacion_invima_ref TEXT,
            notificacion_invima_por TEXT,
            notificacion_distribuidores_at TEXT,
            distribuidores_notificados TEXT,
            notificacion_distribuidores_por TEXT,
            recoleccion_inicio_at TEXT,
            recoleccion_completada_at TEXT,
            cantidad_recolectada INTEGER,
            disposicion_final TEXT
              CHECK(disposicion_final IN ('destruccion','reproceso','devolver_proveedor','cuarentena')
                    OR disposicion_final IS NULL),
            disposicion_descripcion TEXT,
            efectividad_porcentaje INTEGER,
            efectividad_descripcion TEXT,
            estado TEXT NOT NULL DEFAULT 'iniciado'
              CHECK(estado IN ('iniciado','clasificado','invima_notificado',
                                'distribuidores_notificados','en_recoleccion',
                                'completado','cerrado','cancelado')),
            fecha_cierre TEXT,
            cerrado_por TEXT,
            observaciones_cierre TEXT,
            creado_en TEXT NOT NULL DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS')),
            actualizado_en TEXT
        );

-- tabla: recalls_eventos
CREATE TABLE recalls_eventos (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            recall_id INTEGER NOT NULL,
            evento_tipo TEXT NOT NULL,
            estado_anterior TEXT,
            estado_nuevo TEXT,
            usuario TEXT,
            comentario TEXT,
            creado_en TEXT NOT NULL DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS'))
        );

-- tabla: rh_compromisos_mejora
CREATE TABLE rh_compromisos_mejora (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            empleado_id INTEGER NOT NULL,
            evento_origen_id INTEGER,  -- FK rh_eventos (llamado de atencion)
            titulo TEXT NOT NULL,
            descripcion TEXT DEFAULT '',
            tipo TEXT DEFAULT 'reinduccion',
              -- reinduccion | capacitacion_correctiva | seguimiento_disciplinario
            plan_accion TEXT DEFAULT '',
            fecha_compromiso TEXT NOT NULL,  -- fecha de creacion
            fecha_objetivo TEXT,             -- fecha limite cumplimiento
            estado TEXT DEFAULT 'pendiente'
              CHECK(estado IN ('pendiente','en_progreso','completado','vencido','cancelado')),
            video_url TEXT DEFAULT '',  -- link a video IA de reinduccion
            evidencia_url TEXT DEFAULT '',
            firma_empleado TEXT DEFAULT '',  -- nombre o aceptacion
            fecha_firma_empleado TEXT,
            verificado_por TEXT DEFAULT '',
            fecha_verificacion TEXT,
            jefe_responsable TEXT DEFAULT '',
            observaciones TEXT DEFAULT '',
            creado_por TEXT DEFAULT '',
            fecha_creacion TEXT DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS'))
        );

-- tabla: rh_eventos
CREATE TABLE rh_eventos (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            empleado_id INTEGER NOT NULL,
            tipo TEXT NOT NULL,
              -- incapacidad_comun | incapacidad_laboral | accidente_trabajo |
              -- licencia_maternidad | licencia_paternidad | licencia_luto |
              -- licencia_no_remunerada | licencia_calamidad |
              -- vacaciones | permiso_remunerado |
              -- llamado_atencion_verbal | llamado_atencion_escrito |
              -- suspension | felicitacion | reinduccion | otro
            fecha_inicio TEXT NOT NULL,
            fecha_fin TEXT,
            dias INTEGER DEFAULT 0,
            descripcion TEXT DEFAULT '',
            -- Para incapacidades / accidentes:
            diagnostico TEXT DEFAULT '',
            cie10 TEXT DEFAULT '',  -- codigo diagnostico
            entidad_emisora TEXT DEFAULT '',  -- EPS / ARL / hospital
            origen TEXT DEFAULT '',  -- comun | laboral
            -- Para llamados de atencion:
            motivo TEXT DEFAULT '',
            severidad TEXT DEFAULT '',  -- leve | grave | muy_grave
            jefe_id INTEGER,  -- empleado que registra el llamado
            jefe_nombre TEXT DEFAULT '',
            area TEXT DEFAULT '',
            -- Calculo legal de pago:
            salario_diario_referencia DOUBLE PRECISION DEFAULT 0,
            pago_empleador DOUBLE PRECISION DEFAULT 0,
            pago_eps DOUBLE PRECISION DEFAULT 0,
            pago_arl DOUBLE PRECISION DEFAULT 0,
            descuento_nomina DOUBLE PRECISION DEFAULT 0,
            calculo_detalle_json TEXT DEFAULT '[]',
            -- Documentos / soportes:
            documento_url TEXT DEFAULT '',
            -- Estado:
            estado TEXT DEFAULT 'registrada'
              CHECK(estado IN ('registrada','aprobada','rechazada','cerrada','cancelada')),
            aprobado_por TEXT DEFAULT '',
            fecha_aprobacion TEXT,
            observaciones_cierre TEXT DEFAULT '',
            -- Sync con tesoreria/nomina:
            nomina_registro_id INTEGER,  -- FK a nomina_registros si aplica
            sincronizado_tesoreria INTEGER DEFAULT 0,
            -- Meta:
            registrado_por TEXT DEFAULT '',
            fecha_registro TEXT DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS'))
        );

-- tabla: rol_afinidad_config
CREATE TABLE rol_afinidad_config (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            rol_destino TEXT NOT NULL,
            rol_predeterminado TEXT NOT NULL,
            peso INTEGER NOT NULL DEFAULT 1,
            UNIQUE(rol_destino, rol_predeterminado)
        );

-- tabla: rotacion_operarios_state
CREATE TABLE rotacion_operarios_state (
            rol TEXT PRIMARY KEY,
            ultimo_operario_id INTEGER,
            ultimo_asignado_at TEXT,
            actualizado_por TEXT
        );

-- tabla: schema_migrations
CREATE TABLE schema_migrations (
            version     INTEGER PRIMARY KEY,
            applied_at  TEXT    NOT NULL DEFAULT (to_char((now() AT TIME ZONE 'UTC'),'YYYY-MM-DD HH24:MI:SS')),
            description TEXT    NOT NULL DEFAULT ''
        );

-- tabla: security_events
CREATE TABLE security_events (
                 id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
                 ts        TEXT NOT NULL,
                 event     TEXT NOT NULL,
                 username  TEXT,
                 ip        TEXT,
                 user_agent TEXT,
                 details   TEXT);

-- tabla: sgd_capacitaciones
CREATE TABLE sgd_capacitaciones (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            sgd_codigo TEXT NOT NULL,
            sgd_version TEXT NOT NULL,
            persona_username TEXT NOT NULL,
            asignado_at TEXT NOT NULL DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS')),
            leido_at TEXT,
            firmado_at TEXT,
            firma_hash TEXT,
            evaluado INTEGER DEFAULT 0,
            nota_evaluacion DOUBLE PRECISION,
            nota_minima DOUBLE PRECISION,
            estado TEXT NOT NULL DEFAULT 'asignada'
              CHECK(estado IN ('asignada','leida','firmada','aprobada','reprobada','vencida')),
            fecha_limite TEXT,
            asignado_por TEXT,
            UNIQUE(sgd_codigo, sgd_version, persona_username)
        );

-- tabla: sgd_conflictos
CREATE TABLE sgd_conflictos (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            codigo TEXT NOT NULL,
            archivos_detectados TEXT,
            temas_detectados TEXT,
            estado TEXT NOT NULL DEFAULT 'pendiente'
              CHECK(estado IN ('pendiente','en_revision','resuelto','ignorado')),
            resolucion TEXT,
            resuelto_por TEXT,
            resuelto_at TEXT,
            creado_en TEXT NOT NULL DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS'))
        );

-- tabla: sgd_documentos
CREATE TABLE sgd_documentos (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            codigo TEXT NOT NULL UNIQUE,
            area TEXT NOT NULL,
            tipo_doc TEXT NOT NULL,
            numero INTEGER,
            subtipo TEXT,
            titulo TEXT NOT NULL,
            descripcion TEXT,
            padre_codigo TEXT,
            version_actual TEXT NOT NULL DEFAULT '1',
            archivo_pdf_url TEXT,
            archivo_origen TEXT,
            fecha_creacion TEXT,
            fecha_aprobacion TEXT,
            vigente_desde TEXT,
            proxima_revision TEXT,
            estado TEXT NOT NULL DEFAULT 'vigente'
              CHECK(estado IN ('borrador','revision','vigente','obsoleto','retirado','conflicto')),
            elaborado_por TEXT,
            revisado_por TEXT,
            aprobado_por TEXT,
            observaciones TEXT,
            creado_por TEXT,
            creado_en TEXT NOT NULL DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS')),
            actualizado_en TEXT
        );

-- tabla: sgd_versiones
CREATE TABLE sgd_versiones (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            codigo TEXT NOT NULL,
            version TEXT NOT NULL,
            fecha_aprobacion TEXT,
            archivo_url TEXT,
            archivo_origen TEXT,
            motivo_cambio TEXT,
            aprobado_por TEXT,
            creado_en TEXT NOT NULL DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS')),
            UNIQUE(codigo, version)
        );

-- tabla: sgsst_items
CREATE TABLE sgsst_items (
        id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
        categoria TEXT, descripcion TEXT, frecuencia TEXT DEFAULT 'Anual',
        ultimo_cumplimiento TEXT, proximo_vencimiento TEXT,
        responsable TEXT, estado TEXT DEFAULT 'Pendiente',
        creado_en TEXT DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS')));

-- tabla: sign_challenges
CREATE TABLE sign_challenges (
            token TEXT PRIMARY KEY,
            username TEXT NOT NULL,
            auth_factor TEXT NOT NULL,
            created_at_utc TEXT NOT NULL,
            expires_at_utc TEXT NOT NULL,
            consumed INTEGER DEFAULT 0,
            consumed_at_utc TEXT DEFAULT NULL,
            ip TEXT DEFAULT ''
        );

-- tabla: sku_mee_config
CREATE TABLE sku_mee_config (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            sku_codigo TEXT NOT NULL,
            mee_codigo TEXT NOT NULL,
            componente_tipo TEXT NOT NULL DEFAULT 'envase'
                CHECK(componente_tipo IN ('envase','tapa','etiqueta','caja',
                                          'serigrafia','tampografia','plegadiza','otro')),
            cantidad_por_unidad DOUBLE PRECISION NOT NULL DEFAULT 1,
            aplica INTEGER NOT NULL DEFAULT 1,
            notas TEXT,
            UNIQUE(sku_codigo, mee_codigo)
        );

-- tabla: sku_planeacion_config
CREATE TABLE sku_planeacion_config (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            producto_nombre TEXT NOT NULL UNIQUE,
            categoria TEXT,
            cadencia_dias INTEGER,
            cobertura_target_dias INTEGER NOT NULL DEFAULT 60,
            cobertura_max_dias INTEGER NOT NULL DEFAULT 90,
            cobertura_min_dias INTEGER NOT NULL DEFAULT 30,
            merma_pct DOUBLE PRECISION NOT NULL DEFAULT 5.0,
            prioridad INTEGER NOT NULL DEFAULT 5,
            presentacion_default_id INTEGER,
            activo INTEGER NOT NULL DEFAULT 1,
            notas TEXT,
            actualizado_en TEXT, alias_calendar TEXT, estado TEXT NOT NULL DEFAULT 'activo', descontinuado_at TEXT, descontinuado_por TEXT, razon_estado TEXT
        );

-- tabla: sku_precios
CREATE TABLE sku_precios (
                 id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
                 sku TEXT NOT NULL, descripcion TEXT,
                 precio_base DOUBLE PRECISION DEFAULT 0,
                 precio_mayorista DOUBLE PRECISION DEFAULT 0,
                 unidad TEXT DEFAULT 'unidad',
                 empresa TEXT DEFAULT 'ANIMUS',
                 activo INTEGER DEFAULT 1);

-- tabla: sku_producto_map
CREATE TABLE sku_producto_map (
            sku TEXT PRIMARY KEY,
            producto_nombre TEXT NOT NULL,
            activo INTEGER DEFAULT 1
        );

-- tabla: solicitudes_compra
CREATE TABLE solicitudes_compra
                 (id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
                  numero TEXT UNIQUE, fecha TEXT, estado TEXT DEFAULT 'Pendiente',
                  solicitante TEXT, urgencia TEXT DEFAULT 'Normal',
                  observaciones TEXT, aprobado_por TEXT, fecha_aprobacion TEXT,
                  numero_oc TEXT, area TEXT DEFAULT 'Produccion', empresa TEXT DEFAULT 'Espagiria', categoria TEXT DEFAULT 'Materia Prima', tipo TEXT DEFAULT 'Compra', email_solicitante TEXT DEFAULT '', fecha_requerida TEXT DEFAULT '', valor DOUBLE PRECISION DEFAULT 0, influencer_id INTEGER REFERENCES marketing_influencers(id));

-- tabla: solicitudes_compra_anticipada
CREATE TABLE solicitudes_compra_anticipada (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            checklist_item_id INTEGER NOT NULL,
            produccion_id     INTEGER,
            producto_nombre   TEXT NOT NULL,
            tipo_item         TEXT NOT NULL,
            mee_codigo        TEXT DEFAULT '',
            descripcion       TEXT DEFAULT '',
            cantidad_unidades DOUBLE PRECISION DEFAULT 0,
            decoracion_tipo   TEXT DEFAULT '',
            fecha_objetivo    TEXT,
            estado            TEXT DEFAULT 'pendiente'
              CHECK(estado IN ('pendiente','decidida','completada','cancelada')),
            decision          TEXT DEFAULT '',
            decidido_por      TEXT DEFAULT '',
            fecha_decision    TEXT,
            oc_numero         TEXT DEFAULT '',
            tarea_operativa_id INTEGER,
            proveedor         TEXT DEFAULT '',
            observaciones     TEXT DEFAULT '',
            solicitado_por    TEXT DEFAULT '',
            fecha_creacion    TEXT DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS'))
        );

-- tabla: solicitudes_compra_items
CREATE TABLE solicitudes_compra_items
                 (id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
                  numero TEXT, codigo_mp TEXT, nombre_mp TEXT,
                  cantidad_g DOUBLE PRECISION, unidad TEXT DEFAULT 'g', justificacion TEXT, valor_estimado DOUBLE PRECISION DEFAULT 0, precio_unit_g DOUBLE PRECISION DEFAULT 0, proveedor_sugerido TEXT DEFAULT '', actualizado_at TEXT, actualizado_por TEXT DEFAULT '');

-- tabla: solicitudes_produccion
CREATE TABLE solicitudes_produccion (
        id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
        sku TEXT NOT NULL, descripcion TEXT DEFAULT '',
        unidades_solicitadas INTEGER DEFAULT 0,
        motivo TEXT DEFAULT 'Stock bajo',
        estado TEXT DEFAULT 'Pendiente',
        prioridad TEXT DEFAULT 'Normal',
        fecha_solicitud TEXT DEFAULT (to_char(now(),'YYYY-MM-DD')),
        fecha_requerida TEXT DEFAULT '',
        solicitado_por TEXT DEFAULT 'sistema',
        observaciones TEXT DEFAULT ''
    );

-- tabla: stock_pt
CREATE TABLE stock_pt (
                 id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
                 sku TEXT NOT NULL, descripcion TEXT DEFAULT '',
                 lote_produccion TEXT DEFAULT '', fecha_produccion TEXT,
                 unidades_inicial INTEGER DEFAULT 0, unidades_disponible INTEGER DEFAULT 0,
                 precio_base DOUBLE PRECISION DEFAULT 0, empresa TEXT DEFAULT 'ANIMUS',
                 estado TEXT DEFAULT 'Disponible', observaciones TEXT DEFAULT '', stock_minimo_ud INTEGER DEFAULT 0, dias_reposicion INTEGER DEFAULT 15);

-- tabla: tareas_internas
CREATE TABLE tareas_internas (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            titulo            TEXT NOT NULL,
            descripcion       TEXT DEFAULT '',
            estado            TEXT DEFAULT 'Asignada' CHECK(estado IN ('Asignada','EnProceso','Bloqueada','Hecha','Cancelada')),
            prioridad         TEXT DEFAULT 'Media' CHECK(prioridad IN ('Alta','Media','Baja')),
            area              TEXT DEFAULT '',
            origen            TEXT DEFAULT 'manual',
            origen_ref        TEXT DEFAULT '',
            fecha_compromiso  TEXT,
            fecha_creacion    TEXT DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS')),
            fecha_completada  TEXT,
            creado_por        TEXT,
            reincidente_de_id INTEGER,
            notas_avance      TEXT DEFAULT ''
        );

-- tabla: tareas_operativas
CREATE TABLE tareas_operativas (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            titulo            TEXT NOT NULL,
            descripcion       TEXT DEFAULT '',
            tipo              TEXT DEFAULT 'general',
            producto_relacionado TEXT DEFAULT '',
            mee_codigo           TEXT DEFAULT '',
            cantidad             DOUBLE PRECISION DEFAULT 0,
            asignado_a           TEXT DEFAULT '',
            fecha_objetivo       TEXT,
            estado               TEXT DEFAULT 'pendiente'
              CHECK(estado IN ('pendiente','en_progreso','completada','cancelada')),
            origen_tipo          TEXT DEFAULT 'manual',
            origen_id            INTEGER,
            creado_por           TEXT DEFAULT '',
            completado_por       TEXT DEFAULT '',
            fecha_creacion       TEXT DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS')),
            fecha_completado     TEXT,
            observaciones_cierre TEXT DEFAULT ''
        );

-- tabla: tareas_raci
CREATE TABLE tareas_raci (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            tarea_id        INTEGER NOT NULL,
            usuario         TEXT NOT NULL,
            rol             TEXT NOT NULL CHECK(rol IN ('R','A','C','I')),
            asignado_por    TEXT,
            fecha_asignacion TEXT DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS')),
            UNIQUE(tarea_id, usuario, rol)
        );

-- tabla: users_mfa
CREATE TABLE users_mfa (
            username           TEXT PRIMARY KEY,
            secret             TEXT NOT NULL,
            enabled            INTEGER NOT NULL DEFAULT 0,
            backup_code_hash   TEXT,
            created_at         TEXT NOT NULL DEFAULT (to_char((now() AT TIME ZONE 'UTC'),'YYYY-MM-DD HH24:MI:SS')),
            enabled_at         TEXT,
            last_used_at       TEXT,
            disabled_at        TEXT
        );

-- tabla: users_passwords
CREATE TABLE users_passwords (
            username      TEXT PRIMARY KEY,
            password_hash TEXT NOT NULL,
            changed_at    TEXT NOT NULL DEFAULT (to_char((now() AT TIME ZONE 'UTC'),'YYYY-MM-DD HH24:MI:SS')),
            changed_by    TEXT NOT NULL DEFAULT ''
        );

-- tabla: usuarios_identidad
CREATE TABLE usuarios_identidad (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            username TEXT NOT NULL UNIQUE,
            cedula TEXT DEFAULT '',
            nombre_completo TEXT DEFAULT '',
            cargo TEXT DEFAULT 'Por definir',
            area TEXT DEFAULT '',
            email TEXT DEFAULT '',
            manager_username TEXT DEFAULT '',
            activo INTEGER DEFAULT 1,
            created_at TEXT DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS')),
            updated_at TEXT DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS'))
        );

-- tabla: workflow_lunes_log
CREATE TABLE workflow_lunes_log (
            id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
            ejecutado_at TEXT NOT NULL DEFAULT (to_char(now(),'YYYY-MM-DD HH24:MI:SS')),
            ejecutado_por TEXT NOT NULL DEFAULT 'cron-lunes-7am',
            fecha_lunes TEXT NOT NULL,
            producciones_bloqueadas INTEGER DEFAULT 0,
            sincronizadas INTEGER DEFAULT 0,
            asignadas INTEGER DEFAULT 0,
            limpiezas_creadas INTEGER DEFAULT 0,
            email_enviado INTEGER DEFAULT 0,
            error TEXT,
            payload_json TEXT
        );


-- ===== CLAVES FORANEAS =====

ALTER TABLE actividades_sala ADD FOREIGN KEY (area_id) REFERENCES areas_planta (id);
ALTER TABLE actividades_sala ADD FOREIGN KEY (operario_id) REFERENCES operarios_planta (id);
ALTER TABLE actividades_sala ADD FOREIGN KEY (produccion_id) REFERENCES produccion_programada (id);
ALTER TABLE area_eventos ADD FOREIGN KEY (area_id) REFERENCES areas_planta (id);
ALTER TABLE area_eventos ADD FOREIGN KEY (produccion_id) REFERENCES produccion_programada (id);
ALTER TABLE area_eventos ADD FOREIGN KEY (operario_id) REFERENCES operarios_planta (id);
ALTER TABLE bienestar_capacitaciones_intentos ADD FOREIGN KEY (capacitacion_id) REFERENCES bienestar_capacitaciones (id);
ALTER TABLE calidad_oos ADD FOREIGN KEY (capa_id) REFERENCES capa_desviaciones (id);
ALTER TABLE cola_liberacion ADD FOREIGN KEY (envasado_id) REFERENCES produccion_envasado (id);
ALTER TABLE comprobantes_pago ADD FOREIGN KEY (pago_oc_id) REFERENCES pagos_oc (id);
ALTER TABLE control_cambios_eventos ADD FOREIGN KEY (cambio_id) REFERENCES control_cambios (id);
ALTER TABLE cronograma_ejecuciones ADD FOREIGN KEY (cronograma_id) REFERENCES cronogramas_bpm (id);
ALTER TABLE desviaciones_eventos ADD FOREIGN KEY (desviacion_id) REFERENCES desviaciones (id);
ALTER TABLE ebr_ejecuciones ADD FOREIGN KEY (mbr_template_id) REFERENCES mbr_templates (id);
ALTER TABLE ebr_pasos_ejecutados ADD FOREIGN KEY (ebr_id) REFERENCES ebr_ejecuciones (id) ON DELETE CASCADE;
ALTER TABLE ebr_pesajes ADD FOREIGN KEY (ebr_id) REFERENCES ebr_ejecuciones (id) ON DELETE CASCADE;
ALTER TABLE hallazgos ADD FOREIGN KEY (capa_relacionada_id) REFERENCES capa_desviaciones (id);
ALTER TABLE ipc_resultados ADD FOREIGN KEY (ebr_id) REFERENCES ebr_ejecuciones (id) ON DELETE CASCADE;
ALTER TABLE ipc_resultados ADD FOREIGN KEY (ipc_spec_id) REFERENCES ipc_specs (id);
ALTER TABLE ipc_specs ADD FOREIGN KEY (mbr_template_id) REFERENCES mbr_templates (id) ON DELETE CASCADE;
ALTER TABLE maquila_pedidos ADD FOREIGN KEY (cliente_id) REFERENCES clientes_maquila (id);
ALTER TABLE maquila_pedidos ADD FOREIGN KEY (presentacion_id) REFERENCES producto_presentaciones (id);
ALTER TABLE maquila_pedidos ADD FOREIGN KEY (produccion_id) REFERENCES produccion_programada (id);
ALTER TABLE marketing_agentes_feedback ADD FOREIGN KEY (log_id) REFERENCES marketing_agentes_log (id);
ALTER TABLE mbr_pasos ADD FOREIGN KEY (mbr_template_id) REFERENCES mbr_templates (id) ON DELETE CASCADE;
ALTER TABLE produccion_envasado ADD FOREIGN KEY (produccion_id) REFERENCES produccion_programada (id);
ALTER TABLE produccion_envasado ADD FOREIGN KEY (presentacion_id) REFERENCES producto_presentaciones (id);
ALTER TABLE quejas_clientes_eventos ADD FOREIGN KEY (queja_id) REFERENCES quejas_clientes (id);
ALTER TABLE recalls_eventos ADD FOREIGN KEY (recall_id) REFERENCES recalls (id);
ALTER TABLE sku_planeacion_config ADD FOREIGN KEY (presentacion_default_id) REFERENCES producto_presentaciones (id);
ALTER TABLE users_mfa ADD FOREIGN KEY (username) REFERENCES users_passwords (username) ON DELETE CASCADE;

-- ===== INDICES =====

CREATE INDEX idx_aal_prod ON auto_asignacion_log(produccion_id);
CREATE INDEX idx_aal_ts ON asistente_acciones_log(ts DESC);
CREATE INDEX idx_aal_usuario ON asistente_acciones_log(usuario, ts DESC);
CREATE INDEX idx_aca_estado_fecha ON animus_conteos_asignados(estado, fecha_asignado DESC);
CREATE INDEX idx_aca_sku ON animus_conteos_asignados(sku, fecha_asignado DESC);
CREATE INDEX idx_act_area_inicio ON actividades_sala(area_id, inicio_at DESC);
CREATE INDEX idx_act_operario ON actividades_sala(operario_id, inicio_at DESC);
CREATE INDEX idx_act_prod ON actividades_sala(produccion_id);
CREATE INDEX idx_actas_fecha ON comites_actas(fecha DESC);
CREATE INDEX idx_agent_memory_category ON agent_memory(category, updated_at DESC);
CREATE INDEX idx_agent_memory_updated ON agent_memory(updated_at DESC);
CREATE INDEX idx_agua_estado ON calidad_sistema_agua(estado, fecha DESC);
CREATE INDEX idx_agua_fecha ON calidad_sistema_agua(fecha DESC);
CREATE INDEX idx_agua_punto ON calidad_sistema_agua(punto_muestreo, fecha DESC);
CREATE INDEX idx_aim_sku_fecha ON animus_inventario_movimientos(sku, fecha DESC);
CREATE INDEX idx_aim_tipo_fecha ON animus_inventario_movimientos(tipo, fecha DESC);
CREATE INDEX idx_apr_fecha ON auto_plan_runs(ejecutado_at DESC);
CREATE INDEX idx_area_eventos_area_ts ON area_eventos(area_id, ts DESC);
CREATE INDEX idx_area_eventos_prod ON area_eventos(produccion_id, ts);
CREATE INDEX idx_areas_tipo ON areas_planta(tipo);
CREATE INDEX idx_aso_creado ON animus_shopify_orders(creado_en);
CREATE INDEX idx_audit_accion ON audit_log(accion, fecha DESC);
CREATE INDEX idx_audit_registro ON audit_log(registro_id, fecha DESC);
CREATE INDEX idx_audit_usuario ON audit_log(usuario, fecha DESC);
CREATE INDEX idx_autoplan_cliente_fecha
           ON autoplan_decisiones(cliente, fecha_decision);
CREATE INDEX idx_autoplan_producto
           ON autoplan_decisiones(producto_nombre);
CREATE INDEX idx_aze_fecha ON audit_zero_error_runs(fecha DESC);
CREATE INDEX idx_backup_log_started ON backup_log(started_at DESC);
CREATE INDEX idx_bcap_int_cap ON bienestar_capacitaciones_intentos(capacitacion_id, empleado_username);
CREATE INDEX idx_bcapac_fecha ON bienestar_capacitaciones(fecha_limite);
CREATE INDEX idx_bcapac_user ON bienestar_capacitaciones(asignado_a, estado);
CREATE INDEX idx_bridge_bid ON mp_formula_bridge(bodega_material_id);
CREATE INDEX idx_bridge_fid ON mp_formula_bridge(formula_material_id);
CREATE INDEX idx_caja_fecha ON animus_caja_menor(fecha DESC);
CREATE INDEX idx_caja_tipo ON animus_caja_menor(tipo);
CREATE INDEX idx_cal_log_estado ON calendar_eventos_log(estado, fecha);
CREATE INDEX idx_cal_log_producto ON calendar_eventos_log(producto_matcheado, fecha);
CREATE INDEX idx_canonico_config_activo
           ON producto_canonico_config(activo) WHERE activo = 1;
CREATE INDEX idx_capa_estado ON capa_desviaciones(estado, fecha_apertura DESC);
CREATE INDEX idx_capa_nc ON capa_acciones(nc_id);
CREATE INDEX idx_capa_resp ON capa_desviaciones(responsable, estado);
CREATE INDEX idx_ccc_fecha ON conteo_ciclico_calendario(fecha, estado);
CREATE INDEX idx_ccc_fecha_asig ON conteo_ciclico_calendario(fecha, asignado_a);
CREATE INDEX idx_ccc_material ON conteo_ciclico_calendario(material_id, fecha DESC);
CREATE INDEX idx_chat_react_msg ON chat_reactions(message_id);
CREATE INDEX idx_checklist_plant_prod ON checklist_plantillas(producto_nombre);
CREATE INDEX idx_chg_estado ON control_cambios(estado, fecha_solicitud DESC);
CREATE INDEX idx_chg_ev ON control_cambios_eventos(cambio_id, creado_en);
CREATE INDEX idx_chg_invima ON control_cambios(requiere_invima, estado) WHERE requiere_invima=1;
CREATE INDEX idx_chg_responsable ON control_cambios(responsable_implementacion, estado) WHERE responsable_implementacion IS NOT NULL;
CREATE INDEX idx_chg_severidad ON control_cambios(severidad, estado);
CREATE INDEX idx_chg_solicitante ON control_cambios(solicitado_por, estado);
CREATE INDEX idx_cjr_job ON cron_jobs_runs(job_name, ejecutado_at DESC);
CREATE UNIQUE INDEX idx_cjr_unique_ok_today ON cron_jobs_runs(job_name, substr(ejecutado_at,1,10)) WHERE ok=1;
CREATE INDEX idx_cl_estado ON cola_liberacion(estado, fecha_min_liberacion);
CREATE INDEX idx_cl_producto ON cola_liberacion(producto_nombre, lote);
CREATE INDEX idx_cmr_envasado ON calidad_micro_resultados(envasado_id);
CREATE INDEX idx_coa_conforme ON coa_resultados(conforme);
CREATE INDEX idx_coa_lote ON coa_resultados(lote);
CREATE INDEX idx_coa_mp ON coa_resultados(codigo_mp);
CREATE INDEX idx_compmejora_empleado ON rh_compromisos_mejora(empleado_id, estado);
CREATE INDEX idx_compmejora_evento ON rh_compromisos_mejora(evento_origen_id);
CREATE INDEX idx_comprobantes_pago_fecha ON comprobantes_pago(fecha_emision DESC);
CREATE INDEX idx_comprobantes_pago_oc ON comprobantes_pago(numero_oc);
CREATE INDEX idx_contenido_estado ON marketing_contenido(estado);
CREATE INDEX idx_contenido_fecha_prog ON marketing_contenido(fecha_programada);
CREATE INDEX idx_conteo_fecha ON animus_conteos_ciclicos(fecha_conteo DESC);
CREATE INDEX idx_conteo_sku ON animus_conteos_ciclicos(sku);
CREATE INDEX idx_cotizaciones_oc ON cotizaciones(numero_oc);
CREATE INDEX idx_cotizaciones_proveedor ON cotizaciones(proveedor);
CREATE INDEX idx_cotizaciones_ronda ON cotizaciones(ronda_id);
CREATE INDEX idx_cron_ej_estado ON cronograma_ejecuciones(estado, fecha_planeada);
CREATE INDEX idx_cron_ej_id ON cronograma_ejecuciones(cronograma_id, fecha_planeada);
CREATE INDEX idx_dbhealth_fecha ON db_health_log(fecha DESC);
CREATE INDEX idx_despachos_cliente ON despachos(cliente_id, fecha DESC);
CREATE INDEX idx_desv_area ON desviaciones(area_origen, estado);
CREATE INDEX idx_desv_clasif ON desviaciones(clasificacion, estado);
CREATE INDEX idx_desv_detectado ON desviaciones(detectado_por, estado);
CREATE INDEX idx_desv_estado ON desviaciones(estado, fecha_deteccion DESC);
CREATE INDEX idx_desv_ev ON desviaciones_eventos(desviacion_id, creado_en);
CREATE INDEX idx_desv_fecha ON desviaciones(fecha_deteccion DESC);
CREATE INDEX idx_ebr_estado ON ebr_ejecuciones(estado, iniciado_at_utc DESC);
CREATE INDEX idx_ebr_mbr ON ebr_ejecuciones(mbr_template_id, iniciado_at_utc DESC);
CREATE UNIQUE INDEX idx_ebr_numero_op
           ON ebr_ejecuciones(numero_op) WHERE numero_op IS NOT NULL;
CREATE INDEX idx_ebr_pasos_ebr ON ebr_pasos_ejecutados(ebr_id, orden);
CREATE INDEX idx_ebr_pasos_estado ON ebr_pasos_ejecutados(estado, ebr_id);
CREATE INDEX idx_ebr_produccion ON ebr_ejecuciones(produccion_id) WHERE produccion_id IS NOT NULL;
CREATE INDEX idx_empdoc_empleado ON empleados_documentos(empleado_id, tipo);
CREATE INDEX idx_empdoc_venc ON empleados_documentos(fecha_vencimiento);
CREATE INDEX idx_eos_leads_estado ON eos_leads(estado, creado_en DESC);
CREATE INDEX idx_eq_cron_codigo ON equipos_cronograma(equipo_codigo, anio);
CREATE INDEX idx_eq_cron_mes ON equipos_cronograma(anio, mes, estado);
CREATE INDEX idx_eq_ev_codigo ON equipos_eventos(equipo_codigo, fecha DESC);
CREATE INDEX idx_eq_ev_proxima ON equipos_eventos(fecha_proxima);
CREATE INDEX idx_eq_ev_tipo ON equipos_eventos(tipo_evento, fecha DESC);
CREATE INDEX idx_equipos_area ON equipos_planta(area_codigo, activo);
CREATE INDEX idx_equipos_capacidad ON equipos_planta(capacidad_litros);
CREATE INDEX idx_equipos_tipo ON equipos_planta(tipo, activo);
CREATE INDEX idx_esig_meaning ON e_signatures(meaning, signed_at_utc DESC);
CREATE INDEX idx_esig_record ON e_signatures(record_table, record_id, signed_at_utc DESC);
CREATE INDEX idx_esig_signer ON e_signatures(signer_username, signed_at_utc DESC);
CREATE INDEX idx_espec_mp ON especificaciones_mp(codigo_mp);
CREATE INDEX idx_estab_producto ON estabilidades(producto, lote_piloto);
CREATE INDEX idx_feedback_agente ON marketing_agentes_feedback(agente);
CREATE INDEX idx_feedback_log ON marketing_agentes_feedback(log_id);
CREATE UNIQUE INDEX idx_fh_codigo_pt
           ON formula_headers(codigo_pt) WHERE codigo_pt IS NOT NULL;
CREATE INDEX idx_form_ver_id ON formulas_versiones(formula_id, version_num DESC);
CREATE INDEX idx_hall_estado ON hallazgos(estado, fecha_limite);
CREATE INDEX idx_hall_origen ON hallazgos(origen, severidad);
CREATE INDEX idx_inf_metrics_inf_fecha
           ON marketing_influencers_metrics(influencer_id, fecha DESC);
CREATE INDEX idx_influencer_discount_code ON marketing_influencers(discount_code);
CREATE INDEX idx_ipcres_conforme ON ipc_resultados(conforme, ebr_id);
CREATE INDEX idx_ipcres_ebr ON ipc_resultados(ebr_id, ipc_spec_id);
CREATE INDEX idx_ipcspec_mbr ON ipc_specs(mbr_template_id, parametro);
CREATE INDEX idx_limpieza_equipo ON equipo_limpieza_log(equipo_codigo, completado_at_utc DESC);
CREATE INDEX idx_limpieza_lote_sig ON equipo_limpieza_log(lote_siguiente) WHERE lote_siguiente != '';
CREATE INDEX idx_lpc_area ON limpieza_profunda_calendario(area_codigo, fecha DESC);
CREATE INDEX idx_lpc_fecha ON limpieza_profunda_calendario(fecha, estado);
CREATE INDEX idx_lpc_fecha_asig ON limpieza_profunda_calendario(fecha, asignado_a);
CREATE INDEX idx_maestro_mps_tipo_material ON maestro_mps(tipo_material);
CREATE INDEX idx_maq_owner ON maquila_pipeline(owner, stage);
CREATE INDEX idx_maq_stage ON maquila_pipeline(stage, creado_en DESC);
CREATE INDEX idx_maquila_cliente ON maquila_pedidos(cliente_id, estado);
CREATE INDEX idx_maquila_estado ON maquila_pedidos(estado, fecha_entrega_objetivo);
CREATE INDEX idx_maquila_prod ON maquila_pedidos(produccion_id);
CREATE INDEX idx_maquila_producto ON maquila_pedidos(producto_nombre, estado);
CREATE INDEX idx_mbr_estado ON mbr_templates(estado, producto_nombre);
CREATE INDEX idx_mbr_pasos_template ON mbr_pasos(mbr_template_id, orden);
CREATE INDEX idx_mbr_producto_estado ON mbr_templates(producto_nombre, estado);
CREATE INDEX idx_members_thread ON chat_thread_members(thread_id);
CREATE INDEX idx_members_user ON chat_thread_members(username);
CREATE INDEX idx_micro_res_estado ON calidad_micro_resultados(estado, fecha_analisis DESC);
CREATE INDEX idx_micro_res_lote ON calidad_micro_resultados(lote);
CREATE INDEX idx_micro_res_prod ON calidad_micro_resultados(producto_nombre, fecha_analisis DESC);
CREATE INDEX idx_micro_specs_prod ON calidad_micro_specs(producto_nombre);
CREATE INDEX idx_mkt_alerts_at ON marketing_alertas_enviadas(enviado_at DESC);
CREATE INDEX idx_mlt_envase ON mp_lead_time_config(es_envase, activo);
CREATE INDEX idx_mlt_origen ON mp_lead_time_config(origen, activo);
CREATE INDEX idx_mov_fecha ON movimientos(fecha DESC);
CREATE INDEX idx_mov_lote ON movimientos(material_id, lote);
CREATE INDEX idx_mov_material ON movimientos(material_id);
CREATE INDEX idx_mps_fecha ON mp_alcanza_snapshots(fecha DESC);
CREATE INDEX idx_msgs_sender ON chat_messages(sender);
CREATE INDEX idx_msgs_thread ON chat_messages(thread_id, creado_en DESC);
CREATE INDEX idx_msj_a ON mensajes_internos(a_usuario, leido_at);
CREATE INDEX idx_msj_de ON mensajes_internos(de_usuario, fecha DESC);
CREATE INDEX idx_notif_app_dest ON notificaciones_app(destinatario, leido_at, creado_en DESC);
CREATE INDEX idx_notif_app_tipo ON notificaciones_app(tipo, creado_en DESC);
CREATE INDEX idx_notif_emp_estado ON notificaciones_empleados(estado, tipo);
CREATE INDEX idx_notif_emp_user ON notificaciones_empleados(empleado_username, creado_en DESC);
CREATE INDEX idx_oc_estado ON ordenes_compra(estado, fecha DESC);
CREATE INDEX idx_oos_estado ON calidad_oos(estado, fecha_deteccion DESC);
CREATE INDEX idx_oos_lote ON calidad_oos(lote);
CREATE INDEX idx_oos_producto ON calidad_oos(producto);
CREATE UNIQUE INDEX idx_pagos_oc_factura_unique ON pagos_oc(numero_factura_proveedor) WHERE numero_factura_proveedor != '';
CREATE INDEX idx_pagos_oc_numero ON pagos_oc(numero_oc);
CREATE INDEX idx_pc_consumido ON produccion_checklist(produccion_id, consumido_at);
CREATE INDEX idx_pchk_estado ON produccion_checklist(estado);
CREATE INDEX idx_pchk_fecha ON produccion_checklist(fecha_planeada, estado);
CREATE INDEX idx_pchk_prod ON produccion_checklist(produccion_id, item_tipo);
CREATE INDEX idx_pchk_produccion ON produccion_checklist(produccion_id, item_tipo);
CREATE INDEX idx_pe_estado ON produccion_envasado(estado, iniciado_at DESC);
CREATE INDEX idx_pe_lote ON produccion_envasado(lote, producto_nombre);
CREATE INDEX idx_pe_micro ON produccion_envasado(muestra_micro_id);
CREATE INDEX idx_pedidos_b2b_cliente ON pedidos_b2b(cliente_id, estado);
CREATE INDEX idx_pedidos_b2b_estado ON pedidos_b2b(estado, fecha_estimada);
CREATE INDEX idx_pedidos_b2b_producto ON pedidos_b2b(producto_nombre, estado);
CREATE INDEX idx_pedidos_cliente ON pedidos(cliente_id, fecha DESC);
CREATE INDEX idx_pedidos_estado_fecha ON pedidos(estado, fecha DESC);
CREATE INDEX idx_pedidos_items_numero ON pedidos_items(numero_pedido);
CREATE INDEX idx_pesajes_ebr ON ebr_pesajes(ebr_id, material_id);
CREATE INDEX idx_pesajes_lote_mp ON ebr_pesajes(lote_mp) WHERE lote_mp != '';
CREATE INDEX idx_phist_fecha ON precio_historico_mp(fecha DESC);
CREATE INDEX idx_phist_mp_fecha ON precio_historico_mp(codigo_mp, fecha DESC);
CREATE INDEX idx_phist_proveedor ON precio_historico_mp(proveedor);
CREATE INDEX idx_pp_area ON produccion_programada(area_id, fecha_programada);
CREATE INDEX idx_pp_area_env ON produccion_programada(area_envasado_id);
CREATE INDEX idx_pp_bloq ON produccion_programada(bloqueado_at);
CREATE INDEX idx_pp_envase ON producto_presentaciones(envase_codigo);
CREATE INDEX idx_pp_estado_pausa
           ON produccion_programada(estado, motivo_pausa)
           WHERE estado='esperando_recurso';
CREATE INDEX idx_pp_fecha ON produccion_programada(fecha_programada);
CREATE INDEX idx_pp_fecha_estado ON produccion_programada(fecha_programada, estado);
CREATE INDEX idx_pp_fin_real ON produccion_programada(fin_real_at, producto) WHERE fin_real_at IS NOT NULL;
CREATE INDEX idx_pp_gcal ON produccion_programada(gcal_event_id);
CREATE INDEX idx_pp_producto ON produccion_programada(producto);
CREATE INDEX idx_pp_sku ON producto_presentaciones(sku_shopify);
CREATE INDEX idx_pp_workflow ON produccion_programada(semana_workflow_id);
CREATE INDEX idx_presence_estado ON chat_user_presence(estado, last_heartbeat DESC);
CREATE INDEX idx_push_alerts_fecha ON marketing_push_alerts_log(fecha DESC);
CREATE INDEX idx_qc_estado ON quejas_clientes(estado, fecha_recepcion DESC);
CREATE INDEX idx_qc_ev ON quejas_clientes_eventos(queja_id, creado_en);
CREATE INDEX idx_qc_lote ON quejas_clientes(lote, fecha_recepcion DESC);
CREATE INDEX idx_qc_recibido ON quejas_clientes(recibido_por, estado);
CREATE INDEX idx_qc_salud ON quejas_clientes(impacto_salud, estado) WHERE impacto_salud=1;
CREATE INDEX idx_qc_severidad ON quejas_clientes(severidad, estado);
CREATE INDEX idx_quejas_estado ON quejas_internas(estado, fecha DESC);
CREATE INDEX idx_raci_tarea ON tareas_raci(tarea_id);
CREATE INDEX idx_raci_usuario ON tareas_raci(usuario, rol);
CREATE INDEX idx_rcl_clase ON recalls(clase_recall, estado);
CREATE INDEX idx_rcl_estado ON recalls(estado, fecha_inicio DESC);
CREATE INDEX idx_rcl_ev ON recalls_eventos(recall_id, creado_en);
CREATE INDEX idx_rcl_iniciado ON recalls(iniciado_por, estado);
CREATE INDEX idx_rcl_lotes ON recalls(lotes_afectados, fecha_inicio DESC);
CREATE INDEX idx_rheventos_empleado ON rh_eventos(empleado_id, fecha_inicio DESC);
CREATE INDEX idx_rheventos_estado ON rh_eventos(estado);
CREATE INDEX idx_rheventos_tipo ON rh_eventos(tipo, estado);
CREATE INDEX idx_sgd_area ON sgd_documentos(area, estado);
CREATE INDEX idx_sgd_cap_codigo ON sgd_capacitaciones(sgd_codigo, sgd_version);
CREATE INDEX idx_sgd_cap_persona ON sgd_capacitaciones(persona_username, estado);
CREATE INDEX idx_sgd_conf_estado ON sgd_conflictos(estado, codigo);
CREATE INDEX idx_sgd_padre ON sgd_documentos(padre_codigo);
CREATE INDEX idx_sgd_proxima ON sgd_documentos(proxima_revision) WHERE estado='vigente';
CREATE INDEX idx_sgd_tipo ON sgd_documentos(tipo_doc, estado);
CREATE INDEX idx_sgd_ver_codigo ON sgd_versiones(codigo, fecha_aprobacion DESC);
CREATE INDEX idx_shopify_discount_codes ON animus_shopify_orders(discount_codes);
CREATE INDEX idx_shopify_flujo_pending ON animus_shopify_orders(flujo_synced) WHERE flujo_synced=0;
CREATE INDEX idx_signchall_expires ON sign_challenges(expires_at_utc);
CREATE INDEX idx_signchall_username ON sign_challenges(username, expires_at_utc DESC);
CREATE INDEX idx_smc_mee ON sku_mee_config(mee_codigo, aplica);
CREATE INDEX idx_smc_sku ON sku_mee_config(sku_codigo, aplica);
CREATE INDEX idx_sol_estado ON solicitudes_compra(estado, fecha DESC);
CREATE INDEX idx_solprod_estado ON solicitudes_compra_anticipada(estado, fecha_creacion DESC);
CREATE INDEX idx_solprod_producto ON solicitudes_compra_anticipada(producto_nombre);
CREATE INDEX idx_spc_activo ON sku_planeacion_config(activo, prioridad);
CREATE INDEX idx_tareas_asignado ON tareas_operativas(asignado_a);
CREATE INDEX idx_tareas_estado ON tareas_internas(estado, fecha_compromiso);
CREATE INDEX idx_tareas_origen ON tareas_internas(origen, origen_ref);
CREATE INDEX idx_threads_actividad ON chat_threads(activo, ultimo_mensaje_en DESC);
CREATE INDEX idx_users_mfa_enabled ON users_mfa(enabled);
CREATE INDEX idx_usuarios_identidad_activo ON usuarios_identidad(activo, username);
CREATE INDEX idx_usuarios_identidad_manager ON usuarios_identidad(manager_username) WHERE manager_username != '';
CREATE INDEX idx_wll_lunes ON workflow_lunes_log(fecha_lunes DESC);
CREATE UNIQUE INDEX uq_mkt_alerts_dia
           ON marketing_alertas_enviadas(agente, COALESCE(sku,''), COALESCE(tipo_alerta,''), fecha_envio);
CREATE UNIQUE INDEX uq_ordenes_compra_numero ON ordenes_compra(numero_oc);
CREATE UNIQUE INDEX uq_solicitudes_compra_numero ON solicitudes_compra(numero);
