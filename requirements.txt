Flask==3.0.0
gunicorn==21.2.0
python-dotenv==1.0.0
openpyxl==3.1.2
fpdf2==2.7.9
sentry-sdk[flask]==2.60.0
# pyotp: TOTP RFC 6238 para MFA (Google Authenticator, Authy, 1Password)
# Sebastian (30-abr-2026): MFA opcional para todos, recomendado para admins.
pyotp==2.9.0
# qrcode: genera QR server-side para setup MFA (CSP-safe · sin servicio externo)
# Sebastian 13-may-2026: QR escaneable desde iPhone Camera nativa
qrcode[pil]==7.4.2
# psycopg: driver PostgreSQL · migración Fase 3+ · solo se usa cuando
# EOS_DB_BACKEND=postgres (import perezoso) · sin esa env var, no se carga.
psycopg[binary]==3.3.4
